# 结算延迟：服务端与客户端分段定位

## 已验证的结论

20 场 2v2、80 个公网客户端同时进入第 21 回合重负载结算，主要等待发生在服务器排队和模拟阶段。2026-09-26 同机旧版第一轮结果就绪 P95 为 40.68 秒，交付 P95 为 41.58 秒。优化版相同种子第一轮分别为 33.25 秒、34.88 秒，交付约减少 16.1%。不能把两个 P95 相减当成逐玩家的网络耗时。

优化版 5 轮共 100 场战斗，400 次玩家交付确认，0 服务端断开、0 客户端断开、0 应用心跳超时。每个客户端收到双方两份回放，因此记录了 800 次解码；Mac 侧解码 P95 31.396 毫秒，最大 52.991 毫秒。此解码数据不代表手机 CPU 性能。

严格容量检查本次全部 PASS，包括 2.5 秒快速探针门槛；5 轮交付 P95 仍有 32.03–37.64 秒，不是秒结算，也不是最大在线人数保证。服务器为实际核验的 e2-standard-2（2 vCPU、8 GB）。测试未升级硬件。

## 公网对照

| 版本/轮次 | 结果就绪 P95 | 回放交付 P95 | 玩家交付确认 | 服务端断开/客户端断开/心跳超时 |
|---|---:|---:|---:|---:|
| 同日旧版 battle_01 | 40.68 s | 41.58 s | 80/80 | 0/0/0 |
| 优化版 battle_01 | 33.25 s | 34.88 s | 80/80 | 0/0/0 |
| 优化版 battle_02 | 36.84 s | 37.64 s | 80/80 | 0/0/0 |
| 优化版 battle_03 | 32.86 s | 33.74 s | 80/80 | 0/0/0 |
| 优化版 battle_04 | 31.39 s | 32.29 s | 80/80 | 0/0/0 |
| 优化版 battle_05 | 31.21 s | 32.03 s | 80/80 | 0/0/0 |

同种子第一轮累计模拟工作耗时 35.451 → 28.562 秒，约减少 19.4%。公网和虚拟机调度有波动，因此另做了本机交替顺序的 60 场对照：累计模拟耗时三组分别为 5.850→5.302、5.875→5.339、5.721→5.190 秒（约 9%）；完整回放逐字节比较，0 差异。

## 实现

- 攻击阶段每次建立临时 UID/嘲讽索引，避免每个存活单位每个 tick 重复扫描整队。每次重新读取生命、位置及分路约束；保留嘲讽同距离原顺序、锁定优先级、重复 UID 首条语义。索引不跨 tick、房间或任务共享。技能选目标保留原逻辑；召唤与转换后下一攻击阶段重建索引。
- 回放采样减少 Dictionary 重复字段读取；没有改变战斗步长、伤害、碰撞迭代、随机数消耗和回放格式。
- 服务端记录同一单调时钟下的排队、模拟墙钟/实际工作、打包队列/工作及发布等待。发布等待包含打包后的容器释放和主线程领取结果。客户端分别记录解码及验证时间，保留原结构/身份验证。
- 没有增加心跳超时、放宽压测门槛或跳过失败回放。

## 真机客户端诊断

使用已安装的独立 QA 包 com.glory.stabilityqa（运行提交 68e8a624），V2527A 真机，固定种子 20260807、第 21 回合、26 个初始演员。它是离线固定战斗，不能代替 20 房间的真人联机测试。

播放前总准备 18.678 秒，其中特效预热 15.556 秒、模型创建 526 毫秒、资源读取 323 毫秒。17.17 秒播放窗口的真实墙钟帧间隔 P95 19.142 毫秒、P99 21.441 毫秒、最大 55.415 毫秒，0 帧超过 100 毫秒，未出现结果页抢跑。说明这份样本的客户端等待主要在开始播放前的预热，不能据此排除其他机型/阵容的运行中卡顿。

新版 a8930aa 的真机复测通过，APK 安装后 SHA-256 与构建产物一致，资源指纹与旧包一致。相同固定战斗下：

| 客户端样本 | 总准备 | 特效预热 | 播放帧 P95 / P99 / 最大 | 超过 100 ms 的帧 |
|---|---:|---:|---:|---:|
| 旧包，已有着色器缓存 | 18.678 s | 15.556 s | 19.142 / 21.441 / 55.415 ms | 0 |
| 新包，已有着色器缓存 | 15.593 s | 12.499 s | 19.379 / 21.953 / 40.046 ms | 0 |
| 新包，清空 QA 着色器缓存 | 20.958 s | 15.859 s | 19.615 / 24.081 / 58.447 ms | 0 |

已有缓存的同设备样本，准备减少 3.085 秒，约 16.5%；特效预热减少约 19.7%。冷启动仍有约 21 秒准备，不把它与旧版有缓存样本作收益比较。清空的仅是独立 QA 包 shader_cache，不是账号或存档。两次新版测试 manifest 都是 PASS，失败列表为空。三个样本完整回放 hash 一致；新版冷启动播放期间新增着色器源数为 0。

实现按已审查的图层出现时间缩短 7 条预热路线，保留延迟命中、两种光照、材质引用和取消逻辑；未知技能仍走保守窗口。真实渲染检查 52 项通过，独立完整时长运行作为覆盖基准，缩短窗口必须覆盖其所有可见 shader/texture 管线。初版覆盖断言把额外光照覆盖也当作差异而失败，修正为“不允许遗漏”；失败日志保留。没有关闭预热，也没有修改真实战斗特效的时长。

## 证据与边界

原始证据位于忽略目录 reports/settlement-20260926/：public-baseline、public-candidate、compare.log、phone-existing-build、各回归 JSON/日志。公网测试使用现有 tools/run_capacity_remote.py，旧版 1 轮、优化版 5 轮，20 房间、四个独立 Mac 客户端进程各承载 20 个连接；非生产端口和独立用户目录。临时测试服务已停止、副本已移除，来源受限的云防火墙规则及标签已撤销。

本次模拟/网络四个修改文件与完成 5 轮的测试副本 SHA-256 一致。客户端预热优化另行验证，不计入这份服务端收益。

Godot DTLS 销毁时两次运行均有 3 条既有 p_mutex->mutex is null 诊断，均在检查完成后；运行时脚本错误为 0。UDP 接收缓冲告警仍存在：旧版单轮 480 条，新版 5 轮 4,394 条，不能称传输层无丢包；此次可靠回放最终全部交付，应用层掉线为 0。告警尚未逐报文归因，保留原始日志供后续传输优化。

目前的串行模拟仍限制最坏情况集中结算。继续降到几秒需要隔离全局状态后的多进程执行或进一步削减模拟开销，不能直接把依赖 GameState/RNG 的模拟塞进线程池。


## 验证与交付

- 运行提交 `a8930aa98d897c04d387d17f4bd0ce32d30d760d` 已推送 `codex/battle-server-stability`，未合并 main。之后报告补记不改变运行代码。
- Linux 不可变发布包门禁：调度与传输 25、调度器 315、结果 ACK 21、碰撞 55、目标索引 4,816、回放交付 122，共 5,354 项全部通过，0 引擎/脚本错误。真实渲染覆盖 52 项通过。发布助手 32 项故障注入测试通过。
- 服务器 ZIP：`glory_server_p32_20260925T203245Z_a8930aa98d89_3465f5cfceff0ac7_592897c1.zip`，SHA-256 `4d72f1f30a423a8c635b73da949e79b987c49917c9bb81d7c428055875022245`，源树 SHA-256 `3465f5cfceff0ac74aeb35cc4cdd838176099ce9ef137768a69d3b3442537816`。
- 已于北京时间 2026-09-26 04:43 左右更新正式服务。更新前后核验空房间和完整连接日志，PID `948504`，`NRestarts=0`，60 FPS、协议 32、DTLS、12 ms 调度预算/2 ms 时间片/4 活跃任务。自动回滚定时器已取消，临时准入控制已移除。玩家房间快照 hash/mtime 未改变。
- 更新后公网只读探针 PASS：30.007 秒，9 ping/9 pong，最长间隔 3.059 秒，未登录账号、未加入房间。测试结束连接已关闭。
- APK：`Glory-debug-20260926-043245-a8930aa9.apk`，SHA-256 `c728857c0447ba326720579971c4d3bada3380c4d04577a0e06ef271d7775260`，独立 QA 包 `com.glory.stabilityqa`；真机安装文件哈希已核对。
- IPA：`Glory-app-store-20260926-043520-630601-a8930aa9.ipa`，0.0.6（12）、`com.superforge.glory`，SHA-256 `bd32025773651359d66adcbf0cc5fb8625b2bfbf8573034b792a30239e5240fb`；ZIP、签名、描述文件、arm64、包内身份校验通过。
- 两端构建的资源指纹与上一版一致：80 模型、237 表面、325 纹理引用材质核验通过。本轮没有素材更新，不重复上传相同 Drive 增量包。
- **尚未完成：GitHub Release 二进制附件上传、Apple 上传/处理、内部测试组分配。** 登录已确认，但 Chrome 扩展文件上传权限不足，Transporter 文件选择器自动操作没有完成，随后 Mac 锁屏。新包已经准备好，不把包体构建或账号登录计为 TestFlight 交付。

精简证据见 `docs/evidence/settlement-20260926.json`；全部交付路径与校验和保存在外层 `delivery/release-20260926/`。
