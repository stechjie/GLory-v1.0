# 9.12 bug 修复记录（2026-09-13）

项目：GLory-codex。依据桌面《9.12bug提交及修复01.docx》，原问题文档保留不变。

| 问题 | 修改结果 | 检验方法 |
| --- | --- | --- |
| 商店无法取消选中 | 再点同卡取消；关闭及收起清空选择并刷新高亮 | 选中、重复点击、关闭后点棋盘，不应误买 |
| 跳过战斗提前领取金币 | 本人结束回放后发送确认，等服务端进入下一轮备战再应用结算，等待界面显示“需等待其他人战斗结束”；买棋、刷新、祭坛、赌博增加阶段守卫 | 两名在线玩家，一人跳过另一人继续播放，前者不能提前获得收益或进入备战；后者结束后前者正常进入 |
| 打断锁链 / 腐蚀毒针图片 | 两个原图片文件内容对调，名称与画面重新对应 | 图鉴及选宝分别检查锁链与毒针 |
| 宝藏描述不一致 | 打断改缴械；选宝失血补3秒；哀鸣图鉴为15%；胡牌手第五件为狂怒阵容，中英文同步 | 对照选宝和图鉴 |
| 狂暴进攻换目标不重置 | 攻速加成由独立层数计算；换目标清层，不再永久修改基础攻速 | 连击同目标后换目标，攻速回落并重新叠加 |
| 压迫反击对象错误 | 被攻击时给攻击者叠加狂暴进攻，同时施加减攻 | 攻击持有该联动的棋子，攻击者获得攻速倍率 |
| 离线自测说明 | 宝藏列表悬停显示选宝说明；单位列表及已放置格子悬停显示属性和技能，按所选星级格式化 | 鼠标停留于对应控件 |

验证：本轮专项11项通过（含结算等待的隔离状态测试）；上一轮专项42项通过；宝藏既有回归8项通过；OfficeTestSmoke全流程无失败，覆盖场景加载、摆放、模拟、跳过、返回编辑。未进行安卓、真实多客户端网络验收，未导出安装包、未部署服务器。历史README生成的健康报告未重新生成，不将其旧结果当成本轮验证。

部署：按原相对路径同步客户端和Godot服务端，使用Godot重新导入两张图片并重新打包；服务端战斗模拟也需更新并重启。请使用本包“用于覆盖/GLory-codex”内的内容合并至目标项目根目录（project.godot所在目录），不能用整个文件夹替换项目。保留本项目原有其他修改。

还原：桌面包包含此次修改前原件以及一键还原脚本；仅回退本批修改并删除本批新增文件，运行前另存当前版本，不处理安装包、存档和远程部署。

## 本次复查补充

已确认上次改动在GLory-work；本次开始时GLory-codex的15个文件哈希全部仍为修改前版本。现已将全部修改落实到GLory-codex，覆盖包子目录也统一为GLory-codex。另复现PNG正确但Godot导入纹理仍旧的问题：更新图片时间并重新导入后，运行时锁链/毒针显示正确。

复测：图形模式专项15项通过，包含短按处理函数的二次取消、关闭/收起清除、宝藏效果、结算等待与运行时纹理采样比对；既有专项42项和宝藏回归8项通过；离线自测冒烟0失败，退出仍有资源未释放提示。运行时图片及缴械文案截图放在桌面包验证记录。没有进行真实多人或旧服务端联机实测，没有导出新的EXE/APK，也没有重启服务器。

本次4项客户端问题无需依赖服务端新版才能显示修正；联机结算等待复用现有result_ack/房间阶段机制。狂暴进攻、压迫反击的联机模拟仍需同步更新服务端。

覆盖时，将“用于覆盖/GLory-codex”里的内容合并至实际project.godot所在目录，不能再放进多一层GLory-work。覆盖源码不会改变已经导出的EXE/APK：请从本项目重新导出安装包，或在本机双击包内“校验并启动.cmd”，它会先按manifest验证当前项目的20个文件，强制图片重新检查导入，再启动GLory-codex源码项目；任一文件不匹配会停止。启动前关闭之前运行的游戏，避免误测旧进程。

## 结算等待回合校验补修

上轮仅检查server_phase==prep，遗漏了上一回合prep缓存。现在必须同时满足服务端备战阶段且回合号达到completed_round+1才应用战后金币及进入备战；购买、刷新、祭坛和赌博同样核对当前回合。刷新失败回执bad_phase在真实UI处理分支转换为“需等待其他人战斗结束”。不依赖新增服务端协议；仍需旧服务端正常广播下一回合快照。

本轮18项专项检查通过，特意以旧回合prep作为初始状态验证等待不会提前放行，并实际调用刷新回执UI分支；联机经济回归91项通过。测试退出有对象/资源未释放提示，不计作干净退出。未进行实际远程旧服务端多人测试，未更新服务端或导出EXE/APK。源码覆盖后请用校验入口启动或重新导出，既有二进制不会因源码覆盖自动改变。

最终累计17个修改文件、3个新增文件。新增纳入本批还原清单：NetworkService.gd与PrepUI.gd；它们的原件为本次修补前状态，其余文件保留此前原件。

本次结算等待与bad_phase提示补修仅改客户端行为，无需同步或重启服务端；请更新客户端并重新运行/导出。之前狂暴进攻、压迫反击的联机战斗效果修改仍需同步服务端。实际旧服务端多人兼容性尚待实测。
