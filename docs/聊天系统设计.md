# 聊天系统设计

> 2026-09-10。配套文档：`docs/交友系统设计.md`（关系模型、拉黑、presence、
> canonical 对的理由，本系统大量复用）、`docs/玩家资料系统设计.md`（`text_guard`、
> 举报入口）、`docs/账号系统RFC.md`（四层边界、🔴 三条、`session_token` 两层模型）、
> `database/README.md`（迁移规则与代价表）、`docs/CHECKS.md`（门禁）。
>
> 本文记录的是**取舍与理由**，不是接口手册。字段会随实现调整，
> 但下面每一条「为什么」在推翻之前都应该先被读到。

---

## 零、先纠正一条判断，并说明它是怎么被推翻的

### 「不做聊天」—— 作废

`docs/交友系统设计.md` 第七节把聊天列为非目标，理由是
「私聊要消息存储 + 内容审核 + 举报证据链，比交友系统大一个量级」。

**那句话只对私聊成立，对局内/房间聊天不成立。** 局内聊天可以做到不落库、
不审核（只发预设短语 ID）、不碰任何账号字段 —— 它跟交友系统不是一个量级，
是小一个量级。当初把两者捆在一起是想岔了，跟当初把「在线状态」记成
「被 `C14` 卡住」是同一类错误。

### 「第一版不上 WebSocket」—— 提出后当场作废，记录在此

设计过程中先得出过「第一版用轮询、不上 WS」的结论，理由是：② 今天是无状态的、
客户端一条 WS 都没有、移动端长连接不可靠、今天 0 个已发布客户端。
**那个结论的前提是「只做好友私聊」。**

产品范围随后确定为**四条频道**（世界 / 私聊 / 房间 / 局内），前提变了，结论跟着变：

> **世界频道的全部价值是「热闹」。** 轮询间隔即延迟，3 秒一来一回会让对话
> 节奏断掉，然后没人愿意在里面说话，频道就死了。这是**体验问题，不是性能问题**，
> 与在线人数无关 —— 不能用「今天没玩家」来推迟它。

四条频道各自轮询还会让客户端出现四个独立定时器。一条 WS 承载所有频道
是标准做法，**在这个范围下 WS 反而是更简单的那个**。

保留这段记录是因为：下一个人看到「聊天用了 WebSocket」时，
很容易以为它是默认选择。它不是 —— 它是被世界频道**逼**出来的，
如果哪天世界频道被砍掉，这个决定应该重新评估。

---

## 一、四条频道，分别走哪条线

这是本文最关键的一张表。**它们不是一个系统，是四个**，一致性要求、
存储成本、审核义务完全不同：

| 频道 | 传输 | 落库 | 审核强度 | 说明 |
|---|---|---|---|---|
| **世界频道** | ② WebSocket | 7 天（仅为举报） | 🔴 最严 | 主界面那个「聊天」按钮 |
| **好友私聊** | ② WebSocket（同一条连接） | 30 天 / 每会话 100 条 | 🟡 中 | 点对点 |
| **房间聊天**（大厅等人） | 🔴 **③ ENet** | **不落库** | 🟡 中 | `Team3v3Lobby` 左下已预留位置 |
| **局内聊天**（对战中） | 🔴 **③ ENet** | **不落库** | 🟡 中 | 右下角按钮，待做 |

### 🔴 房间/局内聊天为什么必须走 ENet，不能走 ②

看起来 ② 已经知道谁在哪个房间了 —— `player_presence.room_id`，心跳自报的。
拿它做房间频道路由只要一行代码。

**这会直接踩爆 `docs/交友系统设计.md` 第二节那条最硬的红线**：

> 自报只能用于「说谎没有收益」的数据。

谎报房间号进别人的房间频道 = **有收益**。那是一个开箱即用的定向骚扰入口：
随便填一个房间号，就能对着六个陌生人说话，且不需要真的进房间。
这与「谎报房间号让好友进错房间」有本质区别 —— 后者没收益，前者有。

**③ 不存在这个问题**：座位是它分配的（`RoomService` 的 `seat_tokens`），
谁在房间里它自己知道，不需要任何人自报。

所以房间/局内聊天走 ENet 是**唯一既不需要新基础设施、又不破红线**的路。
顺带的好处：**不需要 ②↔③ 通道，座位票仍然不是本系统的前置**
（`docs/账号系统RFC.md` 9.2 仍然未拍板，本系统不推动它，也不被它挡住）。

### 「谁在说话」这件事已经免费有了

`RoomService.public_seat_identity()`（`scripts/multiplayer/RoomService.gd:64`）
已经在房间里同步 `friend_code` / `player_name` / `avatar`。
气泡挂在座位上即可，**不用新增任何字段**。

⚠️ 顺带澄清一条容易误读的红线：RFC 🔴 第 3 条「`NetProtocol` 不加账号身份字段」
指的是 **JWT 那类凭证**，不是公开资料。昵称头像今天已经在 ENet 上传了，
两者不冲突。**判据是「泄漏了会不会丢号」** —— 昵称不会，JWT 会。

---

## 二、传输层：② 的 WebSocket

### 与现状的差别

今天 `AccountManager._request` 是「问一句、答一句、挂断」，每个请求独立，
**② 完全无状态**。WS 打破这一点：服务器必须记住「哪条连接属于哪个玩家」。

| | 现状（HTTP） | 加了 WS 之后 |
|---|---|---|
| 新消息怎么到 | 客户端定时问 | 服务器主动推 |
| 延迟 | = 轮询间隔 | 几十毫秒 |
| ② 有没有状态 | 无 | **有：连接表在进程内存里** |
| 加一个 worker | 直接加 | 🔴 **消息会丢** |

### 🔴 第一版必须钉死单进程单实例

连接表在进程内存里。开两个 uvicorn worker，A 进程上的玩家发消息，
B 进程上的玩家**收不到** —— 不报错，就是收不到。

这与 `backend/app/rate_limit.py` 注释里那个坑是同一个根因（进程内状态），
但后果完全不在一个量级：

| | 多进程后的表现 |
|---|---|
| `rate_limit` | 额度翻倍。软失败，能忍 |
| WS 连接表 | **消息投递不到**。硬失败，且只在扩容那天才暴露 |

**要写进部署文档，并且在代码里加断言**：启动时检测到 workers > 1 直接拒绝启动。
「靠人记得别加 worker」不是方案 —— 这类错误的代价是静默的。

扩容前的正确做法是 Redis pub/sub 或粘性会话。**那是逃生口，不是第一版内容**
（同交友文档第四节第 4 条对 presence 缓存的处理）。

#### 断言怎么做的 ✅ 2026-09-10 `app/single_instance.py`

启动时**抢一个只监听回环的端口**（默认 48099）当令牌，抢不到就拒绝启动。

为什么不是「读一下配置里写了几个 worker」：uvicorn 的 `--workers N` 会 fork 出
N 个子进程，而**子进程看不到这个 N**（没有对应环境变量，`sys.argv` 是主进程的）。
那条路写出来的是**假保护** —— 看着挡住了，实际永远返回 1。

⛔ **绝不要给那个 socket 设 `SO_REUSEADDR`。** 它在两个平台上语义不同：
Linux 上只影响 TIME_WAIT，而 **Windows 上允许直接绑到一个已被监听的端口** ——
设了它，这把锁在开发机上会静默失效，两个进程都以为自己是唯一实例。

### 服务端骨架 ✅ 2026-09-10

`app/realtime.py`（连接表）+ `app/routes/ws.py`（端点 `/v1/ws`）。

**认证走 `Authorization: Bearer` 头，不走 query string。** `?token=xxx` 是
WebSocket 认证最常见的写法，因为浏览器的 `WebSocket` 构造器不让设自定义头 ——
**我们不受那个限制**（客户端是 Godot，`WebSocketPeer.handshake_headers` 可以带）。
query string 会被写进每一层的访问日志，那等于把凭证抄进日志文件。

认证在 `accept()` **之前**完成，不通过就直接 close，未认证的连接不进连接表。

#### 🔴 「同设备重连」与「换设备顶号」必须分开

两者在网络层长得一模一样（都是「同一个账号又连进来一条」），区别只在
`X-Device-Session`：

| 新连接的 device id | 语义 | 旧连接 |
|---|---|---|
| 与旧连接**相同** | 同一台设备重连（切网、锁屏、被系统冻结） | 悄悄断，**不发 kicked** |
| 与旧连接**不同** | 真的在另一台设备登录 | 断开并下发 `kicked` |

不分的后果很具体：**手机切个网、锁屏一会儿再回来，玩家就收到「你的账号在其他
设备登录」。** 他会去改密码、会来问客服，而实际上什么都没发生。移动端重连是
常态不是异常。有测试钉着，做过变异测试。

#### 🔴 被顶掉的旧连接不能把新连接从表里删掉

时序：新连接 register → 旧连接被 close → 旧连接的 handler 醒来 → 走自己的
`finally` → `unregister(自己)`。这时表里那个 key 上挂的**已经是新连接**了。
照 key 删的话，新连接会从表里消失但仍然连着 —— 症状是「顶号之后新设备也收不到
消息」，不报错、连接看着也是通的。所以 `unregister` 要判 `is conn`。同样有变异测试。

### 客户端：一个新的连接状态机

要写重连退避、心跳、token 过期后重连、Android 切后台被杀后恢复。

`scripts/multiplayer/ReconnectService.gd` 与 `ReconnectBackoff.gd` 已经为 ENet
写过一遍这类逻辑，**思路可以抄，代码不能共用**（一个 ENet 一个 WebSocketPeer）。
抄的时候要连同它们踩过的坑一起抄，别只抄结构。

#### ✅ 2026-09-11「token 过期后重连」补上了 —— 而且顺带修了整个账号层

access token 一小时过期（`jwt_verify.py`：`exp = iat + 3600`），而 `AccountManager` 登录之后
**从来没有续过期**：开着游戏满一小时，好友列表、在线心跳全部开始 401。私聊让它显形 ——
WebSocket 断线重连时拿着过期令牌会永远握手失败。

- **按需续期**：每次带令牌的请求之前按**墙钟**判断是否快过期（手机切后台时 Timer 会停，不能用 Timer）；
  仍然 401 就续一次、重试一次（重试那一趟 `allow_refresh=false`，不会无限递归）
- **单飞**：Supabase 的 refresh token 会轮换，两次并发续期拿同一张旧票会被当成重放
- 续期被 401 拒 = 凭证失效：**不在会话中途改走匿名注册**（那等于游戏开着开着换了号），下次启动再说
- `RealtimeService` 建连前先 `ensure_fresh_token()`；连续两次握手失败就强制续一次 ——
  服务端在 accept 前拒绝时客户端只看得到「握手失败」，分不出是令牌还是网络

### 部署

Caddy 反代 WebSocket 是原生支持的，`deploy/Caddyfile` 需要确认没有会掐长连接的
超时配置。这是上线前必须实测的一条 —— 本机跑通不代表过了反代还通。

---

## 三、数据库

按 `database/README.md` 的规矩，**编号只增不改**。本系统占 `007_chat.sql`。

> 下面几段 SQL 是设计时的草稿，**以 `database/007_chat.sql` 为准**
> （实现时加了 `client_msg_id` 去重、组合外键，去掉了 `deleted_at`）。

### ✅ 2026-09-11 拍板：存多少、存多久

**只存能显示的那部分。** 玩家只能看到、也只能举报自己看得到的消息，不显示的就没有保留的理由
（原来的「30 天 + 每会话 100 条」到这里作废）：

| 情况 | 存多少 | 理由 |
|---|---|---|
| 仍是好友 | 每对最近 **200** 条，不按时间删 | 就是界面能显示的全部；第 201 条写入时同一事务删掉最老的 |
| 解除好友 / 拉黑之后 | 会话对双方消失；记录从最后一条算起**再留 30 天**后整段删除 | 只防「骂完立刻删好友」：记录不能跟着好友关系一起没了。举报是另一个系统，以后接 |
| 被拉黑后发的 | **不落库** | 见第八节第 7 条 |

- **容量有结构性上界**：一个号最多 100 个好友 × 200 条 = 2 万条（最坏约 18 MB），与发多少无关。
  所以**不需要每日发送配额**；每分钟 30 条只防刷屏。剩下的只有「注册大量小号」这种攻击，
  归注册限流与封号管。
- 会话**刻意不外键到好友关系**：外键 + 级联会让「删好友 = 删记录」成立，正是上面「再留 30 天」要防的。
  过期清理在 `backend/app/maintenance.py`（进程内每小时一轮，单实例保证只有一份在跑）。
- 讨论过「为什么不干脆存 30 天」：玩家只能举报看得到的，举报那一刻由**服务器**把当前记录复制一份
  就够了（客户端上传的可以伪造）。为「刷屏把证据挤掉」这种边角情况存所有人 30 天不划算 ——
  被挤掉的消息玩家自己也看不到了，本来就举报不了。
- ⚠️ **国内上线前要找合规确认**：《网络安全法》第二十一条要求网络日志留存不少于六个月，
  《互联网信息服务管理办法》第十四条要求电子公告类服务保存信息内容记录 60 日。它们是否覆盖
  游戏内私聊、要保存到什么程度，**没有核实**。若要求保留，就另建一个只给合规用的存档表
  （不动这套表），届时每日配额要加回来。
- 玩家可见的承诺（聊天界面左栏底部）：「与每位好友只保留最近 200 条消息；解除好友后，记录最多再保留 30 天。」

### 私聊会话：canonical 对，理由同 `player_friendships`

```sql
create table chat_conversations (
  low_id          uuid not null references players(player_id) on delete cascade,
  high_id         uuid not null references players(player_id) on delete cascade,
  last_message_id bigint,
  updated_at      timestamptz not null default now(),
  primary key (low_id, high_id),
  constraint ordered check (low_id < high_id)
);
```

**为什么不用「每人一行」**：那样能表示出「A 的会话里有 5 条、B 的会话里有 3 条」
这个坏状态。写漏不报错，表现是「他看到的和我看到的不一样」。
canonical 对让这个坏状态**不可表示** —— 与交友文档第三节同一条理由。

### 消息

```sql
create table chat_messages (
  message_id  bigserial primary key,
  low_id      uuid not null,
  high_id     uuid not null,
  sender_id   uuid not null references players(player_id) on delete cascade,
  body        text not null,
  created_at  timestamptz not null default now(),
  deleted_at  timestamptz,
  constraint sender_is_party check (sender_id in (low_id, high_id))
);
create index chat_messages_by_conv on chat_messages (low_id, high_id, message_id desc);
```

#### 🔴 游标必须是 `bigserial`，不能用时间戳

增量拉取（「给我 message_id > X 的」）的正确性**全押在游标单调上**。
时间戳会撞（同毫秒两条）、会因为时钟回拨倒退。

这一条事后改是 🔴：所有客户端的本地游标全部作废，
而症状是「偶尔丢一条消息」或「偶尔重复一条」，极难复现、极难定位。
**一次做对，没有第二次机会。**

#### 🔴 `deleted_at` 软删，不是真删

举报单条消息没有用，必须带前后上下文。玩家点「删除聊天记录」时服务端
**不能真删** —— 否则当事人可以自己销毁证据。

代价是一条必须写下来的隐私义务：消息保留 30 天，举报后延长保留。
这句话要出现在玩家可见的地方。

> ✅ **2026-09-11 已定：不做「清空聊天记录」**，所以 007 里**没有 `deleted_at`**。
> 顺带记下一个坑：上面这个 schema 只有**一个** `deleted_at`，它只能表示「对双方都删了」——
> A 清空，B 那边也跟着没了，骚扰者就能在被举报前抹掉受害者看到的记录。
> 以后真要做，只能是「只删自己这边」：每人存一个「我清到第几条」的水位线，
> `deleted_at` 留给违规处理。保留期也改了，见本节开头「存多少、存多久」。

### 已读状态：存游标，不存计数器

```sql
create table chat_read_state (
  player_id    uuid not null references players(player_id) on delete cascade,
  low_id       uuid not null,
  high_id      uuid not null,
  last_read_id bigint not null default 0,
  primary key (player_id, low_id, high_id)
);
```

#### 🔴 未读 = `max(message_id) > last_read_id`，绝不存 `unread_count`

可变计数器在任何一次并发、重发、乱序下都会漂，**而且不报错** ——
只表现为「红点消不掉」或「有新消息不亮」。

这与 `player_presence` **没有「离线」字段**是同一条教训：
**不存会说谎的状态，存能推出真相的事实。**

#### 更新必须是单调的

```sql
update chat_read_state set last_read_id = greatest(last_read_id, $1) ...
```

`greatest()` 不能省。同一账号两台设备同时在线时，设备 B 的一次滞后写会把
游标从 100 退回 50，表现为「已经看过的消息又变成未读」。
写成单调之后，**两台设备读同一个服务端游标，天然一致** ——
「在 A 设备看过了，换到 B 设备也是看过的状态」自动成立，不需要额外机制。

### 世界频道：**不进上面这套表**

世界频道是广播，塞进 `chat_messages` 会让那张表变成一张只增的巨表，
而且 99% 的行永远不会被任何人查第二次。

```sql
create table world_messages (
  message_id bigserial primary key,
  sender_id  uuid not null references players(player_id) on delete cascade,
  body       text not null,
  created_at timestamptz not null default now()
);
create index world_messages_recent on world_messages (message_id desc);
```

- **显示**走 ② 进程内的环形缓冲（最近 50 条），不查库
- **落库只为举报**，保留 7 天，定期清理
- 新玩家进频道时下发那 50 条打底 —— **人少时频道是空的，玩家会以为坏了**

---

## 四、内容审核

这是整个系统真正的关键路径，**不是 WebSocket**。

### `text_guard` 的三层，能复用哪些

`backend/app/text_guard.py` 第一句话写着「这不是敏感词系统，不要这么叫它」。
那句话是对的，逐层拆：

| 层 | 用在聊天上 | 理由 |
|---|---|---|
| ① 结构管控（零宽 / Zalgo / bidi / 空白 / 长度） | ✅ **直接复用，且必须用** | 与内容无关，纯防显示破坏。聊天里同样有人会用 Zalgo 糊屏幕 |
| ② 引流检测（微信 / QQ / 长数字） | ⚠️ **只对世界频道用** | 见下 |
| ③ 词表 | ❌ **不能当唯一防线** | 昵称是低频、一人一个、可人工审；聊天是高频海量。同一份词表会同时漏（谐音拆字随便过）和误杀（正常对话被拦，玩家不知道为什么） |

### ✅ 已定：私聊**不拦**引流，世界频道**拦**

这与签名栏的策略**相反**，是刻意的 —— 场景不同：

| | 性质 | 引流的危害 |
|---|---|---|
| 签名 / 世界频道 | **广播**，一个人发给所有看到的人 | 代练与外挂广告的主战场，必须拦 |
| 私聊 | **点对点**，对方已经同意加好友 | 交换联系方式是正常社交行为，硬拦很烦人 |

私聊的引流内容**记录但不拦，靠举报后处理**。这是已确认的产品决定。

### 外部审核：选型与封装

**选阿里云内容安全（文本审核）**，理由：

1. ② 是 Python，阿里云有官方 Python SDK，也提供纯 HTTP API —— **不依赖任何
   Godot 侧支持**，审核全在 ② 完成
2. 按调用量计费、有免费额度，起步成本接近 0；今天 0 玩家时基本不花钱
3. 中文游戏黑话（谐音、拆字、变体、插符号）是它的主场，这正是词表做不到的部分
4. 文档与案例最多，出问题时能搜到东西

⚠️ **价格与具体产品名会变，接入前必须自己去核实一次**，本文不承诺这些。

#### 🔴 但比选哪家更重要的是：封装成 provider 接口

```
app/moderation/__init__.py     # check(text, scene) -> Verdict
app/moderation/local.py        # text_guard 结构层 + 词表（永远在线，第一道）
app/moderation/aliyun.py       # 外部服务（可插拔，第二道）
```

三个理由，每个都会真的发生：

- **换厂商**：效果或价格不满意时，只改一个模块，不改调用点
- **降级**：外部服务超时/欠费/故障时，聊天不能整个瘫掉
- **测试**：门禁不能真打外部 API，必须能注入假 provider

#### 🔴 外部服务挂了怎么办 —— 按频道分级，不能一刀切

| 频道 | 策略 | 理由 |
|---|---|---|
| 世界频道 | **fail-closed**（拒发，提示稍后重试） | 一条违规全服可见 + 会被截图传出去。宁可发不出去 |
| 私聊 / 房间 / 局内 | **fail-open**（放行，标记待审） | 影响面小到一个人或六个人，且聊天完全不能用比漏一条严重得多 |

**这个分级必须显式写在代码里**，不能是「默认行为恰好如此」——
默认行为会在重构时悄悄翻转。

#### 同步调用，第一版不做异步

发消息时等审核结果返回（约 100–300ms）。代价是发消息的 P99 取决于外部服务，
收益是不存在「违规内容先送达再撤回」这个状态。

聊天不是每秒几百条的东西，这点延迟玩家感知不到。
**量真上来了再改异步，那时候再说。**

### 频率控制

| 频道 | 限制 | 理由 |
|---|---|---|
| 世界频道 | ✅ **CD 8 秒**（已定） | 广告刷屏是必然，不是可能 |
| 世界频道 | ✅ **等级门槛**（已定，见下） | 挡批量小号 |
| 私聊 | 不设 CD，每分钟上限 30 条 | CD 会让正常对话很难受 |
| 房间 / 局内 | 每座位轻 CD（2 秒） | 由 ③ 判，防刷屏 |

#### 🔴 世界频道的门槛判在数据库上，不能用 `rate_limit.py`

`rate_limit.py` 是进程内滑动窗口，**重启即清零**，它自己的注释写着这一点。
用它做 8 秒 CD 是可以的（重启清零无所谓），但用它做**每日发言配额**不行。
这与「改名冷却判在数据库」「好友请求配额判在数据库」是同一条教训。

更硬的一条：🔴 **按 `player_id` 限流，不能按 IP**。理由同交友文档第四节第 2 条 ——
手机运营商 NAT 下同一出口 IP 是成片的正常玩家。

#### 等级门槛：现在还没有等级制度，怎么预留

✅ 已定：用等级门槛，但等级制度尚不存在。

**预留不等于什么都不做。** 直接留一个永远为真的开关等于没留 ——
它会在等级上线那天被发现根本没接过线。做法：

1. 代码里就有一个 `can_speak_in_world(player) -> bool` 的判据函数，
   **今天就调用它**，走通整条路径
2. ✅ 已定：**今天的实现直接返回 `True`，不挡任何人。**
   曾考虑用「账号创建满 24 小时」当临时判据，**否决了** —— 今天 0 玩家、
   没有广告号，那条限制唯一的实际效果是挡住自己人的测试账号。
   真正需要时再换实现（现成的判据是 `players.created_at`，广告号的共同特征
   正是「刚建的」）。
3. 等级制度上线、或真的出现广告号后，只改这个函数体，调用点一处不动

**函数与调用点今天就存在**，所以这条路径是活的、被测过的，
不是一个等着被想起来的 TODO —— 需要它的那天只是换一个函数体，
而不是在一堆调用点里找该往哪插。

---

## 五、单点登录（顶号）

✅ 已定：要做。

### 它是被聊天逼出来的，但它属于账号层

聊天是第一个让「同一账号两台设备」显形的功能，但顶号本身是账号系统的能力，
实现应该落在 `AccountManager` / `routes/auth.py`，**不要写进聊天模块**。

### 🔴 JWT 是无状态的 —— 顶号有一个不可消除的时间窗

`backend/app/jwt_verify.py` 的文件头写着：令牌 `exp = iat + 3600`，
**ES256 非对称验签，后端不需要任何密钥**。这个设计很好，但它的直接后果是：

> **签发出去的 access_token，在过期前无法被撤销。**

撤销 Supabase 的 refresh_token 只能阻止旧设备**续期**，
它手上那个 access_token 在剩余寿命内（最长 1 小时）**仍然验得过**。

要做到即时撤销，只有两条路，都有代价：

| 路 | 代价 |
|---|---|
| 每个请求查一次「当前活跃会话」 | 每个接口多一次 DB 往返。为了顶号给所有接口加成本，不划算 |
| **只对长连接即时生效** | 顶号在聊天上立刻可见，HTTP 接口有 ≤1 小时残留 |

**选第二条。** 具体形状：

1. 登录时 ② 生成 `device_session_id`，记为该账号的**当前活跃设备**
2. 客户端建 WS 时带上它，② 校验
3. 新设备登录 → 旧设备的 WS 被**服务端主动断开**，下发 `kicked` 事件
4. 客户端收到 `kicked` → 🔴 **弹一个需要手动确认的对话框**，
   玩家点「确定」之后才回登录界面

#### 🔴 绝不能自动跳回登录界面

✅ 已定。玩家正在玩，画面突然自己跳回登录页，**在他眼里就是崩溃或掉线**，
不是「我被顶号了」—— 他会去截图报 bug，而不是去想「谁登了我的号」。

必须满足三条：

- **必须手动点确认才走**，不做倒计时自动跳转
- 提示语要说清楚是**什么事**：「你的账号在另一台设备上登录了」，
  而不是「连接已断开」「登录已失效」这类什么都没说的话
- **对局中被顶号不要立刻弹**（弹窗会直接打断操作），先在聊天入口挂一个提示，
  回到主界面时再弹。战斗连接本来就不受这套机制影响，见下

因为客户端最终会被送回登录界面，那个 ≤1 小时的 HTTP 残留窗口在正常路径上
**不会被用到**（只有手动构造请求的人能用）。**这个边界要写下来**，
不能让下一个人以为顶号是即时且彻底的。

✅ **2026-09-11 实现时的偏差：今天没有登录界面**（匿名账号自动登录）。确认之后停在原地：
聊天保持断开，在聊天界面里点「在本设备重新连接」才会再连（`ChatService.reconnect_here`）。
不能做成「确认后自动重新登录」—— 那会立刻把另一台设备顶下线，两台设备来回互踢。
接了 OAuth 之后（批次 F）再换成真正的「回登录界面」。

⚠️ **顶号只作用于 ①↔② 这条链路，不碰 ③ 的座位。** `session_token` 仍然是
「这条连接拥有哪个座位」，与账号是两回事（RFC 第八节的两层模型不变）。
被顶号的设备如果正在对局中，它的战斗连接**不会**被这套机制断掉 ——
那属于座位票的范畴，仍然未拍板。

### 🟡 今天它没有触发条件，但架子现在就要留

匿名账号的凭证是本机 `user://glory_account.json` 里那个 refresh_token，
**只有一份、只在这台设备**（交友文档第一节第 ② 条讲得很清楚）。
所以今天事实上很难出现「两台设备同一账号」。

**真正的触发条件是绑定登录（Google / Apple OAuth）。**
顶号的完整实现可以排在 OAuth 之后，但 `device_session_id` 这个字段
**现在就要加进 WS 握手**：事后往一个已经上线的握手协议里插字段，
要处理新旧客户端并存，比现在多写三行贵得多。

---

## 六、UI 位置

### 主界面：「聊天」按钮 ✅ 2026-09-11

原计划它是世界频道入口。批次 C 先让它进**私聊界面**（`scenes/menu/ChatScreen.gd`）——
私聊上线后这个按钮还写着「敬请期待」会像没做完；世界频道（批次 E）以后作为同一个界面里的
页签加进来。图标右上角挂未读红点，显隐只跟 `ChatService` 走。

### 私聊界面（方案一）✅ 2026-09-11

- 左栏是**全部好友**：聊过的按最后一条消息排前、带红点，没聊过的排后面 —— 这里兼做选人。
  好友列表每一行也有「私聊」直达（从那里进来的返回回好友列表）。
- 按钮一律实例化 `ui/components/GloryActionButton.tscn`，不写 `Button.new()`（V3 P1-08 棘轮）。
- 发送中 / 发送失败显示在气泡下方，失败可「重发」—— 重发复用同一个 `client_msg_id`，对方不会收到两条。
- 被顶号时消息区上方一条原因 +「在本设备重新连接」，输入框禁用；**不自动重连**。

### 房间大厅：位置已预留 ✅

`scenes/menu/Team3v3Lobby.gd:244`：

```gdscript
_add_texture(TEX_CHAT, Vector2(80, 704), Vector2(430, 210), "left")
_add_label(_room_text("目前暂无聊天功能", "Chat coming soon"), Vector2(80, 780), ...)
```

参考画布 `REF_SIZE = 1672×941`，这块是 **430×210，锚定屏幕左边**，
落在 `DEBUG_BANDS` 的「底部：敌方名牌 / 聊天 / 开始」带内（y=677, h=264）。

⚠️ `Team3v3Lobby.gd:716` 的布局校验注释写着「左列最宽的是聊天框(147+432)」——
**聊天框是左侧锚定列的宽度基准**。往右扩会改变 `edge=left` 那条分界线，
进而影响所有左锚元素。要扩宽度前先看那段 F3 调试绘制。

**430×210 能放下什么**：4 行消息（28 高）+ 底部一条短语入口，正好填满。
大厅聊天本来就是短句。不要试图在这里塞频道切换页签。

#### 短语面板：从聊天框顶部往上弹 ✅ 2026-09-10

样式与备战期那块**共用同一份代码** —— 按钮直接调 `PrepWidgets.make_menu_button()`
（那是个「全静态、不持界面状态」的工具箱，注释写着「仿主界面样式」，
在大厅用它是回到本源）。共用之后不存在「改了一边忘了另一边」。

宽度刻意收到 360（比聊天框的 430 窄）：再宽就会盖到敌方席位 1 的左半边
（`SLOT_POS[3]` 的 x 是 447）。

⚠️ 面板与 12 个按钮**在 `_build` 期就建好、默认隐藏**，不是点开时才创建。
`_layout()` 只给 `_placed` 里登记过的控件定位与缩放，而登记发生在创建时 ——
点开时才 `new` 的控件会停在左上角、原始尺寸。这就是 `_ready()` 里那句
「放在 `_layout()` 后面创建的标签会停在默认位置、看不见」说的坑。

⚠️ 复用 `PrepWidgets.make_menu_button()` 有一个必须清掉的东西：它会设
`custom_minimum_size = size`，而 `Control.size` 的 setter 会把值 clamp 到那个下限 ——
于是窗口缩小（scale < 1）时 `_layout()` 设的尺寸被顶回原值，按钮不缩小、整块散开。
备战期那边不用清，它走的是容器布局。

#### 出图工具：`tools/chat_ui_capture.tscn`

不带 `--headless` 跑，输出 `reports/chat_ui/*.png`（收起态 + 展开态各一张）。

它抓到了一件门禁永远抓不到的事：**文字区贴着聊天框边缘时，会被贴图自带的边框
装饰压住**。第一版写 714，第一行上半截被压；726 仍蹭到；732 才干净。左边距同理，
96 时首字紧贴边框，收到 104。这些在代码里完全看不出来，只有出图才看得见 ——
同 `tools/ui_gallery_capture.gd` 那句「检查验的是合同，它永远回答不了『好不好看』」。

⚠️ **2026-09-11 补一刀**：732 / 104 只让开了边框。贴图左上角还有一个**对话气泡图标**
（约 x 97~130、y 725~762），第一行开头两个字一直压在它上面 —— 原尺寸截图里它像边框的一部分，
放大两倍才看清。消息那 4 行因此整体右移到 x=138（`CHAT_MSG_X`，宽 358，右边界仍是 496）；
下面的入口行不动（图标够不到那一行）。

#### 自由文字入口（批次 D）✅ 2026-09-11

- 底部入口行对半分：左「＋ 快捷短语」、右「＋ 打字」（`CHAT_ENTRY_SPLIT` = 196）。
  贴图右下角那个黄色箭头**划进「打字」的判定区**（批次 A 时归短语）—— 它长得就像「发送」
- 点「打字」弹出 `ui/components/ChatInputBar.gd`：**贴屏幕顶部**的输入条，走 `ModalStack`
  （id `chat_text_input`）。🔴 不能贴着聊天框放：手机键盘从底部弹起、盖住下半屏，
  输入条放在下面等于打字时看不见自己打了什么
- 一条消息按消息列的实际宽度**逐字符折行**后进 4 行历史（静态函数 `wrap_chat_text`，门禁直接调它量最坏情况），
  4 行放不下时最老的行先出去。折行带中文排版的「避头」：逗号、句号、冒号这类标点不许顶在行首，
  碰到时把上一行最后一个字一起带下来。不改用 Label 自动折行 —— 那会动到上面出图验过的 4 行固定版面
- 发送方**不抢先回显**，等服务器广播回来再显示（同短语：服务器是唯一定序者，
  抢先显示会让自己看到的顺序和别人不一样）

### 局内（备战期）：右下角按钮 ✅ 2026-09-10

在 `scenes/prep/PrepUI.gd` 的 `_build_chat_entry()`。与主界面**用同一张贴图**
（`assets/ui/main_menu_live/chat.png`），132×132，右下角锚定。

#### 🔴 右下角的真正约束：两套锚打架，而它只在矮窗口下暴露

第一版把聊天贴着屏幕右缘、按钮锚在 `offset_top = -340`（想着避开占 -200 ~ -70
的商店刷新按钮）。**在参考画布 941 高下毫无问题，出图也看不出任何毛病。**

1280×720 下它盖住了萝卜计数器，消息条压在萝卜按钮旁边。原因是两边用的锚不同：

| | 锚 | 占位 |
|---|---|---|
| `side_col`（佣兵 / 队伍佣兵 / 萝卜 / 萝卜计数） | **屏幕顶部**往下 | 固定排到 y=518 |
| 聊天入口 | **屏幕底部**往上 | 随窗口高度浮动 |

**窗口越矮，两者越近。** 941 高时相隔 80 多像素，720 高时直接叠上。

⚠️ 这类 bug 有一个共同特征：**在开发者的窗口尺寸下永远不出现**。
所以调这几个数时必须用矮窗口出图，不能用参考画布的高度 ——
`tools/chat_ui_capture.tscn` 的备战那一段因此写死 1280×720。

最终位置（`PrepUI` 的 `CHAT_RIGHT` / `CHAT_BTN_TOP`）：

- **横向**右边界收到 -148，让开那一列（`STATS_BTN_SIZE.x` 140 + 贴边 8）
- **纵向**按钮压到 -200 ~ -68，与商店刷新按钮同一水平带 —— 那是右下角唯一
  本来就安全的高度（518 之下）。横向已经错开（刷新在 -105 ~ 25），所以并不会
  真的叠上刷新
- 短语面板**往上弹**，与消息条同列同宽。不往左弹：那会横穿到棋盘中央，
  往上只压掉自己那几条消息，代价最小

#### z_index：商店开着时聊天就该点不到

聊天按钮 z=20，商店弹窗 40、卖出区 50。**这是有意的** ——
那时玩家在买卖，一个压在商店上面的聊天按钮只会造成误触。

#### 三条交互约束（都已实现）

- 🔴 **备战阶段不能挡棋盘。** 这是玩家操作最密集的阶段（拖棋子、买卖）。
  入口是折叠的：平时只有一颗按钮，短语面板点开才有，发完自动收起。
  **不上遮罩** —— 萝卜营地那种全屏 dimmer 在这里是错的，
  玩家要能一边看着棋盘一边发短语。
- 🔴 **消息条 `mouse_filter` 必须是 `IGNORE`。** 它压在棋盘右下方的空域上，
  而且是**自动出现**的。少了这一行，一条飘过的消息会把它盖住的那格棋盘
  变成点不动的 —— 玩家只会觉得"卡了"，且只在有人正好说话时发生，极难复现。
  有门禁钉着（`tools/chat_check.tscn`）。
- 🟡 **不用弹窗、不用音效。** 备战期被打断是玩家最容易生气的事。

#### 一处与原设计不同：**没有未读红点**

原设计写的是"按钮 + 未读小红点，点开才看得到消息"。实现时改成
**收到的消息常驻显示在按钮上方并自动淡出**（3 条，停 6 秒后淡出 1 秒）。

理由：局内的聊天是"瞥一眼"，不是"读列表"。要玩家在摆棋的时候先看见红点、
再点开面板、再读一句"稳住"，那句话早就没有意义了。消息直接飘出来之后，
未读红点这个概念本身就不需要了。

⚠️ 每条消息**各自管自己的寿命**（tween 挂在那条 Label 上），不能共用一个 Timer ——
共用的话后到的消息会重置前一条的计时，表现为"一直有人说话时最早那条永远不消失"。

#### 自由文字（批次 D）✅ 2026-09-11

- 入口在短语面板**最上面一行**「＋ 打字」（宽 326 = 两列短语 160×2 + 间距 6），
  面板因此从 282 高到 326。不另加按钮：右下角已经被刷新按钮、side_col 和聊天按钮挤满（见上表）
- 点它先收起短语面板，再弹出与大厅**同一个**顶部输入条（`ChatInputBar`）
- 消息条**按折行后的合计行数管高度**：最多 3 条、合计最多 6 行（`CHAT_LOG_TEXT_LINES`），
  超了从最老的一条开始**整条**移走。🔴 **不许用 `max_lines_visible` 截断** —— 第一版设了 2，
  而 24 字昵称 + 40 字在 352 宽里要 4 行，后半截被悄悄吞掉：读的人只看到半句话，还不知道少了。
  门禁 `tools/chat_check.tscn` 钉着这一条

### 局内（战斗阶段）：✅ 已定**暂不做**（2026-09-10）

`BattleScreen` 是另一个场景、另一套 UI。协议层已经通了，真要接也只是再连一次
`team_chat_received` 加一个按钮 —— 但产品上决定先不做，备战期够用。

---

## 七、实施顺序

| 批次 | 内容 | 传输 | 前置 |
|---|---|---|---|
| **A** | 局内 / 房间**快捷短语 + 预设表情**。✅ **2026-09-10**：协议层、房间大厅、备战期入口、门禁、协议顶号 22。✅ **2026-09-11 ③ 已重新部署到 p22**（用户确认）。预设表情缺贴图素材；战斗阶段入口已定暂不做 | ③ ENet | 无 |
| **B** | ② 的 WebSocket 基础设施（握手、认证、心跳、`device_session_id`）。✅ **2026-09-11**：服务端（`single_instance.py` / `realtime.py` / `routes/ws.py`）+ 客户端状态机（`RealtimeService.gd`）+ 门禁。✅ **接线同日完成**：登录成功后 `start()`、被顶号弹窗、令牌续期（见第十一之二节） | ② | 单实例断言 |
| **C** | 好友私聊（落库、游标、拉黑交互）。✅ **2026-09-11 代码完成**：`007_chat.sql`、`/v1/me/chats` 四个接口 + 推送、`ChatService` + 聊天界面、门禁。🔴 **待做：在 Supabase 跑 007、部署 ②、双号真机验证**。「清空聊天记录」已定不做 | ② WS | B |
| **D** | 房间 / 局内**自由文本**。✅ **2026-09-11 代码完成**：`ChatText.gd` 文本规则、两条 RPC（协议顶到 **24** —— 同一天四星升阶也写了 23，合并时撞号，改成 24，见 `NetworkConfig.gd` 的 v24 注释）、大厅与备战期的「＋ 打字」、顶部输入条 `ChatInputBar`、门禁。**不接外部审核、fail-open**（2026-09-11 拍板：先不连阿里云）。🔴 **待做：重新打包部署 ③ 到 p25** —— 在那之前，新客户端连线上服务器会在握手时被 `protocol_mismatch` 拒掉（**整个联机都进不去，不只是聊天**），所以 ③ 与客户端要一起发 | ③ ENet | ~~审核 provider 接通~~ 无（fail-open） |
| **E** | 世界频道 | ② WS | C + 审核 provider + 门槛判据 |
| **F** | 顶号完整实现 | ② | B（字段）+ OAuth（触发条件） |
| **G** | 语音 | ③ ENet（自建） | ✅ **2026-09-13 游戏内 v1 代码完成**（方案 ②：安卓插件 + ③ 只转同队 + 大厅 / 备战期按钮，协议 25）。🔴 待做：部署 ③ p25、出带语音插件的包、同事真机测回声（第九节） |

**A 与 B 可以并行** —— 它们一条线都不共用。

~~**D 和 E 都卡在同一件事上：内容审核 provider。**~~ 2026-09-11 改：**只有 E 卡在 provider 上**
（世界频道 fail-closed：provider 不在就不能发）。房间 / 局内 / 私聊都是 fail-open，provider 没接也照常工作，
所以 D 先按「只跑本地结构规则」上了。provider 接通后三条 fail-open 的频道再补外部审核，
届时要先回答「房间 / 局内的审核在哪跑」（见第十一之二节第 ④ 条）。
阿里云内容安全的接入方式与价格仍然要尽早核实 —— 它决定 E 能不能按计划走。

### 批次 A 为什么便宜到不成比例

发的不是文本，是**短语 ID**：`{seat: int, phrase_id: int}`。

- 内容审核归零 —— 短语表自己写，审一次管终身
- 不碰 RFC 🔴 第 3 条 —— 载荷里只有座位号和一个 int
- 不需要 ②↔③ 通道 —— 转发全在 ③ 内存里
- 载荷上界天然存在 —— 符合 `NetProtocol.gd:12` 那条「任何容器必须常数级步数被拒」
- 「谁说的」免费 —— `public_seat_identity` 已经在同步昵称

**预设表情与快捷短语是同一件事**（发 ID，不发内容），一起做。

⚠️ **快捷短语不是自由文本的替代品，是补充。** 做了 D 之后 A 也不作废 ——
对战中打字太慢，手机上尤其。LoL 有信号轮盘也有聊天框，两个都要。

---

## 八、容易写错的地方

### 🔴 0. 加 `@rpc` 方法必须顶协议号，并重新部署战斗服务器

**2026-09-10 实测踩过，放在第一条。**

批次 A 加了两个 `@rpc` 方法（`_rpc_team_chat_submit`、`_rpc_team_chat`）却没顶
`NETWORK_PROTOCOL_VERSION`。客户端连线上服务器时直接刷：

```
process_simplify_path: The rpc node checksum failed.
Make sure to have the same methods on both nodes. Node path: NetworkService
```

Godot 会比对两端 `NetworkService` 的方法表，对不上就拒绝为这个节点建立路径缓存。

**但这条错误本身还不是最要命的。** 它背后的事实是：两端方法表已经不一致，
于是 `v17` 注释里那件事同时成立 —— **方法编号错位，RPC 可能被派发到别的方法上**。

⚠️ 方法表不一致时，联机行为是**未定义的**。
不要试图靠观察症状去判断「是不是还能用」—— 那是在拿静默错算结果赌运气。

`NetworkConfig.gd` 的 `v17` 注释早就写过同一条（「加 `@rpc` 方法会平移整套 RPC 的
wire ID」），`v18` 那一格更是**补顶的**。这是第三次踩，所以这次加了断言
（`tools/chat_check.gd` 的 `PINNED_RPC_COUNT`）——踩过三次的东西该由断言挡着，
不该靠记性。

**两步都要做，缺一不可：**

1. 顶 `NETWORK_PROTOCOL_VERSION`（21 → 22 ✅ 批次 A；22 → 23 是同事的四星升阶；23 → 24 ✅ 批次 D —— 批次 D 原本也写的 23，与四星同一天撞号，合并时改成 24），并同步
   `carrot_online_check.gd` 的 `PINNED_PROTOCOL`、
   `chat_check.gd` 的 `PINNED_RPC_COUNT` / `PINNED_RPC_PROTOCOL`（批次 D 并入四星升阶之后是 57 / 24；语音之后是 59 / 25）
2. **重新打包部署战斗服务器**：`make_server_zip.ps1`（会从源码实读协议号，
   产出 `glory_server_p<协议号>.zip`，语音之后是 `glory_server_p25.zip`，
   并做冷启动冒烟测试），然后上传部署

⚠️ **顶号本身不会消除那个 checksum 错误。** 它只让版本语义正确、让旧客户端在握手时
拿到一个可读的 `protocol_mismatch`。**真正的修复只有重新部署服务器。**

⚠️ 第 2 步机器验不了。唯一的证据是服务器启动日志里的 `server started protocol=25`
（批次 A 那次是 22；`journalctl -u glory-server`）—— 注意是 **started**（过去式），不是 starting。

### 🔴 1. 房间频道用 ② 的自报 `room_id` 路由

见第一节。**这条被突破时不会报错**，只会凭空造出一个定向骚扰入口。
房间成员关系只有 ③ 知道，这是它必须走 ENet 的全部理由。

### 🔴 2. 未读用计数器

见第三节。症状是「红点消不掉」，而代码看起来是写了的。

### 🔴 3. 游标用时间戳

见第三节。症状是「偶尔丢一条 / 重复一条」，几乎不可能复现。

### 🔴 4. WS 上了之后有人加 worker

见第二节。要有启动断言，不能靠人记得。

### 🔴 5. 房间/局内自由文本不落库 → **无法举报**

这是批次 D 一个必须正面回答的问题，**很容易漏掉**。

不落库是对的（那是它便宜的全部原因），所以举报材料只能由**举报者的客户端**
上传本地保留的最近 N 条上下文 —— LoL 的做法也是这个。

**代价必须写下来：举报材料由举报方提供，原理上可伪造。** 只能靠人工判断，
以及「伪造聊天记录去举报」本身可罚。想避开这一点只有让 ③ 落库，
那需要 ②↔③ 通道 —— 不值得为举报去开那条通道。

✅ **2026-09-11 批次 D 按「不落库」实现。** 客户端也**没有**为举报额外保留上下文 ——
举报是另一个系统（2026-09-11 定：以后再做），届时再定要不要让客户端留最近 N 条、留在哪。
今天大厅框里的 4 行、备战期消息条的 6 行只是显示用的，离开界面就没了。

### 🔴 6. 「在线可见性」与「消息可达性」被混为一谈

✅ 已定：**隐身的人照样收私聊。**

presence 的隐身开关管的是「别人能不能看到我在线」，
与「消息能不能送到我这里」是两个概念。混在一起会造出
「我隐身了所以朋友的消息我收不到」这种没有人能理解的行为。

写代码时的具体表现：投递逻辑**绝对不能查 `presence_visibility`**。

### 🟡 7. 被拉黑者发消息：✅ 已定**静默丢弃**

对发送方显示为「已发送」，实际不投递。理由同交友文档第六节第 6 条
「删好友不通知对方」—— 告诉对方「你被拉黑了」等于制造对抗，而对方也做不了什么。

~~⚠️ 静默丢弃的消息**仍然落库**（举报证据），只是不投递、不进对方的未读。~~

✅ **2026-09-11 改为不落库。** 拉黑会在同一事务里删掉好友关系（`friends.block()`），
会话随之对双方都不可见 —— 这些消息谁都看不到、也没人能举报，存了只会被人拿来灌库
（让自己的小号拉黑自己，再对它狂发）。

- 发送方看到的响应与真发出去的一模一样：`message_id` 照样从同一个序列里取
  （`chat._silently_drop`），否则 0 或 null 一眼就能认出来
- 判定顺序是**拉黑先于好友关系**：反过来的话被拉黑的人只会拿到「你们不是好友」，
  这条规则就永远走不到
- 已知的代价（2026-09-11 讨论过，接受）：被删好友的人发消息会得到「你们不是好友」，
  被拉黑的人却显示「已发送」—— 仔细对比的人能分辨出自己是被拉黑了。
  交友文档本来就接受「被拉黑的人能从别处推断出来，只是不该由我们主动告知」

### 🟡 8. 客户端本地缓存

第一版**只做内存缓存，不落 `user://`**。落盘就要处理「本地缓存与服务端游标
不一致」，而那个不一致只在换设备、清数据、消息被删这些边角场景出现 ——
最难测、最容易错。

代价是每次开面板都要拉一次最近 N 条。可以接受。

### 🟡 9. 世界频道人少时是空的

0 人在线 = 空频道 = 玩家以为坏了。用那 50 条环形缓冲打底。
**这不是可选的润色，是必须的** —— 空白界面在玩家眼里就是 bug。

### 🟢 10. 举报与拉黑在 UI 上要分开

同交友文档第六节第 7 条。举报是给我们看的、异步的；拉黑是即时生效的。

### 🟢 11. 消息长度上限

世界频道 100 字，私聊 200 字，**房间 / 局内 40 字**（2026-09-11 批次 D；原写 200）。
响应体和 UI 布局都需要一个上界，理由同「好友数上限 100」。

房间 / 局内收到 40 是因为版面：大厅消息列一行约 18 字、只有 4 行，备战期消息条合计 6 行，
200 字一条就能把两处刷满好几遍。对局里的聊天是「瞥一眼」，长段落本来就没人读。
上限是 `ChatText.MAX_CHARS` 一个常量，输入框的 `max_length` 与服务端的 `ChatText.clean` 用的都是它。

---

## 九、语音：单独说，因为它比整个文字聊天加起来还贵

✅ 产品上要做，但**不进本轮排期**，理由如下。

### 自己做：在 Godot 里没有便宜的路

需要麦克风权限、实时 Opus 编解码、抖动缓冲、**回声消除（AEC）**、噪声抑制。
后两项是关键 —— 移动端外放不做 AEC 会直接啸叫。这些在 Unity / UE 有成熟轮子，
Godot 基本要自己啃或写 GDExtension。是几个月的工程，且大概率做不好。

### 接第三方：正常选择，但有一个真实的未知

腾讯云 GME、声网 Agora、ZEGO 都是成熟方案，**但它们官方支持 Unity / UE / 原生，
Godot 通常不在列表里**。安卓上要通过 Godot Android Plugin 走 JNI 自己封一层。
**能不能通、要多久，今天没有人知道。**

外加：按分钟计费的流量成本，以及语音内容审核（实时转文字）是另一项收费服务。

### 🔴 建议：先做一天的 timebox 技术验证

照 `docs/交友系统设计.md` 第八节对 OAuth 的处理 —— 只验一件事：

> **能不能在 Godot 安卓包里跑通一次第三方语音 SDK 的进出房间。**

把未知变成已知，比把它排进计划表有用，也比做到一半发现卡住便宜得多。
验证通过之前，语音在计划表上只能是一个问号，不能是一个日期。

### 2026-09-11 调研：两条路

**A. TwoVoIP（开源 GDExtension）+ 按住说话 + ③ 转发**

- [two-voip-godot-4](https://github.com/goatchurchprime/two-voip-godot-4)，MIT。Opus 编解码（README：约 1 kb/秒）、
  可选 RNNoise 降噪、SpeexDSP 自动增益；按住说话与声控两种模式；GDScript 写的 `VoipHelper` 带噪声门与抖动缓冲
- 最新 v6.5（2026-09-08）。发布物只有一个 `TwoVoIP.zip`，**119,596,329 字节（约 114 MB）**，
  发布说明写 Linux / Windows / macOS / iOS / Android / Web 的构建都通过了。只把要的平台拷进 `addons/`
- 🔴 **按 Godot 4.6 的 API 编译**（godot-cpp 10.0.0-rc1）。GDExtension 通常是「旧版编的能在新版引擎跑」，
  但 4.7.1 能不能加载**必须实测**，不能靠推断
- 🔴 **没有回声消除（AEC）**。缓解：只做按住说话，并且**按住期间本地把别人的语音静音**（半双工）——
  自己说话时扬声器不出声，就不会有回声。代价是说话时听不到别人，对 3 人小队可以接受
- 它不管网络：本地吐出 Opus 包（`transmit_audio_packet` 信号），收到的包交给 `receive_audio_packet`。
  传输走 ③ 的 ENet，只转给同队、座位由 ③ 从 sender 反查（同自由文字），③ 只转发不解码。
  20 ms 一帧是每人每秒 50 包，而 ③ 是单线程主循环，要实测；帧长拉到 40–60 ms 包数能砍一半以上
- 不管用 `@rpc` 还是 `send_bytes` 自定义包，③ 都要重新部署；用 `@rpc` 还要顶协议号（第八节第 0 条）
- 不要账号、不按分钟计费。语音不落地、无法举报（同第八节第 5 条），所以必须有「屏蔽某人的语音」

**B. 商用 SDK（声网 Agora / 腾讯 GME / ZEGO）**

- 有 AEC、降噪、弱网对抗，效果成熟
- 都**没有官方 Godot 插件**：要自己写 Godot Android 插件（包 AAR）+ 开 Gradle 构建；
  包体变大（看所选 SDK 的精简包）；按分钟计费；需要账号与 AppID；语音交给第三方要写进隐私政策

**两条路都要的**：安卓 `RECORD_AUDIO` 权限（导出预设勾选 + 运行时 `OS.request_permission`）、
项目设置打开 `audio/driver/enable_input`、Google Play 数据安全表单申报麦克风、隐私政策加一段。

**建议先验 A**（一天）：① 桌面 4.7.1 能加载、本地回环听得到；② 安卓真机能录、能编解码、能回放；
③ 量 CPU 与延迟。② 过不去就换 B。（↑ 已被下一节推翻：要的是直接开麦。）

### 2026-09-11 定：要能直接开麦（全双工），走方案 ②

用户要的是**直接开麦对话**，不是按住说话。这推翻了上面「建议先验 A」：开麦时队友的声音从扬声器出来、
又被自己的麦克风收进去，**没有回声消除就是回声和啸叫**，A 路那个「按住期间静音别人」的办法不成立。

剩下两条路：② 自建 + 借手机自带的回声消除；B 商用 SDK。用户选 ②（不花钱、不依赖第三方）。
2026-09-13 用户进一步定：**不单独做验证页，直接做进游戏里，由同事在真实房间里测** ——
测的就是验证本来要回答的问题（回声、游戏声漏音），测不过再转 B。
🔴 代价写下来：**测不过的话，③ 的转发和编解码都会作废**（商用 SDK 自带传输），只有按钮和状态机能留下。

查实的事实：
- Godot 自带的安卓录音（OpenSL ES 驱动）**没有设任何录音模式**，拿不到系统回声消除 ——
  必须自己写安卓插件，用 `VOICE_COMMUNICATION` 录、并进 `MODE_IN_COMMUNICATION`
- 对方的声音也要用通话流（`USAGE_VOICE_COMMUNICATION`）放，回声消除才知道扬声器在放什么
- 游戏音乐 / 音效仍走 Godot 的普通播放 —— 有的手机的回声消除不算它，会漏进麦克风（测试重点之一）

### 游戏内语音 v1（2026-09-13 已写好）

| 部分 | 位置 | 做什么 |
|---|---|---|
| 安卓插件（Java，不需要 NDK） | 源码 `android_plugins/glory_voice/` → `addons/glory_voice/bin/GloryVoice.aar`。`build_aar.ps1` 打包，**先跑桌面自测 `VoiceSelfTest`，不过不打包** | 通话模式录音 + 系统回声消除 / 降噪 / 自动增益；判断有没有在说话（自适应噪声底、400 毫秒拖尾、40 毫秒预录）；IMA ADPCM 编码（16 kHz，每 20 毫秒一块，每块自带解码起点，丢包不连累后面）；两帧一包；每个队友一条抖动缓冲（攒 60 毫秒起放、最多压 400 毫秒）；混音后用通话流播放；切后台全停 |
| ③ 转发 | `NetworkService._rpc_team_voice_submit` / `_rpc_team_voice`，`unreliable_ordered` + 独立通道 `CH_VOICE = 2` | 不解码、不看内容；座位由 ③ 从 sender 反查；包大小上限 512 字节；限流 10 秒 300 包（软限，不计 strike）；**只转同队**（`voice_recipients`） |
| 状态机 | `scripts/autoload/VoiceService.gd` | 三档：关 / 只听 / 开麦；离开房间 3 秒自动关；从后台回来恢复原档位；没有麦克风权限时先进「只听」等系统弹窗 |
| 界面 | 大厅聊天框正上方、备战期聊天按钮左边，各一个按钮 | 点一下切一档；文字后面的「●」= 有队友在说话，或自己开麦且正在说 |
| 导出接线 | `addons/glory_voice/glory_voice_plugin.gd` | Android 预设的 `glory_voice/enabled`（默认关）与 Use Gradle Build 同时勾上才打进包 |
| 门禁 | `tools/voice_check.tscn` | 插件包与源码指纹对账、清单、默认不打包、转发契约、只转同队、Java 包长 ≤ ③ 上限、状态机、界面接线 |
| 协议 | v25 | 加了两条 RPC → **③ 要重新部署到 p25** |

带宽：每个正在说话的人约 66 kbps 上行（331 字节 × 每秒 25 包，不含包头），没人说话时不发包；
③ 转给同队另外两人，服务器出口约 132 kbps / 说话人。

#### 🔴 这台电脑出安卓包的坑（2026-09-13 实测）

系统代码页是 1252（英文），**Java 工具收到带中文的路径就打不开**：
1. 签名文件在 `桌面\key\` 下 → Godot 调 keytool 核对签名时打不开文件，报的却是
   「Release Username and/or Password is invalid」，**跟密码无关**。解决：签名文件放到纯英文路径，
   或用环境变量 `GODOT_ANDROID_KEYSTORE_RELEASE_PATH` 指向英文路径 / 8.3 短路径
2. 项目在 `桌面\` 下 → 开了 Gradle 构建后 gradlew 报 `Could not find or load main class org.gradle.wrapper.GradleWrapperMain`。
   解决：从纯英文路径打开项目再导出（2026-09-13 建了目录联接 `C:\Users\stech\GLory-v1.0-link`，指向同一个项目，不是副本）
3. 语音插件必须 Gradle 构建：`android/build` 构建模板要与编辑器版本一致（2026-09-13 重装为 4.7.1；
   旧的 4.7.0 模板挪到了桌面 `GLory-android-4.7.0-backup`）。第一次 Gradle 构建会下载约 1 GB

#### 同事测试步骤

1. ③ 先部署 p25，所有人装新包（旧包连不上新服务器）
2. 同一个房间、同一队的两三个人：各自点「语音：关」→「只听」→「开麦」（第一次会弹麦克风权限）
3. 都**开外放、开着游戏音乐**，轮流说话，听：有没有自己声音的回声（最关键）、有没有听到对面的游戏音乐、
   延迟大不大、断不断续
4. 再各测一次：蓝牙耳机、有线耳机、切到后台再回来（切后台会停，回来自动恢复原档位）、
   **敌方队伍的人听不到**（只转同队）
5. 每台记下机型、安卓版本、现象；有问题连电脑抓 `adb logcat -s GloryVoice godot`

**判定**：主力测试机上外放开麦 + 放着游戏音乐时，对方听不到明显回声、也听不到游戏音乐 →
继续 ②（下一步 Opus 压缩、屏蔽某人、战斗界面入口）；否则转 B。

#### v1 没做的（已知）

- **屏蔽某人**：插件已有 `setRemoteMuted`，界面还没接。**上线前必做** —— 语音不留记录，没法举报
- 战斗界面没有按钮（档位跨场景保持，进战斗前开着的会一直开着）
- 插拔耳机不会重新选输出设备（开会话那一刻选定）
- 切到后台就停（后台继续通话要前台服务）
- ADPCM 的带宽约是 Opus 的 3 倍

---

## 十、非目标（明确不做）

- **不做已读回执。** ✅ 已定。它需要的实时性正好是这套架子最贵的那一档，
  而产品价值很低。
- **不做玩家上传图片。** 预设表情 🟢 很便宜（发 ID），玩家上传 🔴 很贵：
  对象存储 + CDN + 流量费 + **图片审核必须接第三方**（比文字更难更贵）
  + 举报要留原图。**两者差 100 倍，别因为都叫「图片」就当成一件事。**
  这是内容风险最高的功能，没有之一。
- **不做自定义多人群组。** 房间聊天已经覆盖「一起玩的人一起说话」这个需求，
  而自定义群组会把关系模型从 pair 变成 N 方，数据模型完全不同。
- **不做聊天记录搜索、不做消息撤回、不做消息转发。**
- **不缓存、不上 Redis。** 同交友文档第四节第 4 条 —— 那是真上量后的逃生口。
- **不碰 ②↔③ 通道。** 本系统四条频道全部避开了它。

---

## 十一、已定的决策

| 项 | 决定 | 日期 |
|---|---|---|
| 频道范围 | 世界 / 私聊 / 房间 / 局内，四条 | 2026-09-10 |
| 传输 | 世界+私聊走 ② WebSocket；房间+局内走 ③ ENet | 2026-09-10 |
| 私聊引流检测 | **不拦**，记录 + 举报后处理 | 2026-09-10 |
| 世界频道引流检测 | 拦 | 2026-09-10 |
| 消息保留 | ~~30 天 + 每会话最多 100 条~~ → **仍是好友时每对最近 200 条；解除好友后再留 30 天**；世界频道 7 天 | 2026-09-11 |
| 删除方式 | ~~软删（`deleted_at`）~~ → **不做「清空聊天记录」** | 2026-09-11 |
| 被拉黑者发消息 | **静默丢弃**：显示已发送、**不落库**、不投递 | 2026-09-11 |
| 隐身与私聊 | **隐身照样收私聊** | 2026-09-10 |
| 已读状态 | 服务端游标 + `greatest()` 单调更新 | 2026-09-10 |
| 已读回执 | 不做 | 2026-09-10 |
| 局内短语 | 不落库，中途加入看不到历史 | 2026-09-10 |
| 世界频道 CD | **8 秒** | 2026-09-10 |
| 世界频道门槛 | **等级门槛**；判据函数与调用点今天就有，实现暂时返回 `True`（不挡人），真正需要时再换 | 2026-09-10 |
| 单点登录（顶号） | **要做**，载体是 WS 连接，接受 HTTP ≤1 小时残留 | 2026-09-10 |
| 被顶号的表现 | 🔴 **必须手动确认后才回登录界面**，不自动跳转、不倒计时 | 2026-09-10 |
| 内容审核 | 阿里云内容安全，封装成 provider；世界 fail-closed / 其余 fail-open | 2026-09-10 |
| 语音 | 要做，但先做一天 timebox 验证，不排期 | 2026-09-10 |
| 语音形态 | **要能直接开麦（全双工）**，不是按住说话 → 必须有回声消除；先验方案 ②（自建 + 手机自带的回声消除），验不过转商用 SDK（第九节） | 2026-09-11 |
| 语音做法 | **不做单独验证页，直接进游戏由同事测**；测不过转商用 SDK（③ 转发与编解码届时作废） | 2026-09-13 |
| 语音范围 | **只转同队**，敌方永远收不到；房间大厅与备战期有按钮，战斗界面暂无（档位跨场景保持） | 2026-09-13 |
| 语音默认 | 默认「关」；三档循环 关 / 只听 / 开麦；离开房间 3 秒自动关；**麦克风绝不自己打开** | 2026-09-13 |
| 语音编码 | v1 用 IMA ADPCM（16 kHz，约 66 kbps / 说话人），测过回声后再换 Opus | 2026-09-13 |
| 玩家上传图片 | 不做 | 2026-09-10 |
| 私聊界面位置 | 主界面「聊天」按钮进私聊界面（左栏全部好友 + 右侧消息）；好友列表每行「私聊」直达。世界频道以后作为同一界面的页签 | 2026-09-11 |
| 私聊发送 / 接收 | 发送走 HTTPS（明确答复、复用错误表与限流），接收走 ② 的 WS 推送；正确性靠游标补拉 | 2026-09-11 |
| 私聊文本规则 | 结构层照开；**不拦引流、不套昵称词表**；换行压成空格、ZWJ 去掉；200 字 | 2026-09-11 |
| 每日发送配额 | **不做**：容量已被「每对 200 条」结构性封顶；每分钟 30 条只防刷屏 | 2026-09-11 |
| 私聊举报 | 另一个系统，以后接；入口在资料页。届时由服务器在举报那一刻快照当前记录 | 2026-09-11 |
| 房间 / 局内自由文字 | **40 字**；座位号由 ③ 从 sender 反查；**不落库**；③ 每人 10 秒 3 条（软限：超了只丢这一条，不计 strike、不断线），客户端 4 秒一条（4 × 3 ≥ 10，正常发送碰不到服务端的限） | 2026-09-11 |
| 房间 / 局内审核 | **先不接外部审核（fail-open）**，只跑 `ChatText` 的本地结构规则：压缩空白、去 ZWJ、拒不可见 / 双向 / 控制字符、拒连续 3 个及以上的组合附加符号（zalgo）。provider 接通后再补，届时要定「审核在哪跑」 | 2026-09-11 |
| 审核 provider 的时机 | 不再挡 D：房间 / 局内 / 私聊都 fail-open。**世界频道（fail-closed）上线前必须接通** | 2026-09-11 |
| 自由文字输入位置 | 贴屏幕顶部的输入条（`ChatInputBar` + `ModalStack`）：手机键盘从底部弹起，输入条不能在下半屏 | 2026-09-11 |

---

## 十一之二、当前进度（2026-09-11）

> 这一节是**接手点**。要继续做的人先读它，再看第七节的批次表。

### 已经能跑的

| | 状态 |
|---|---|
| 批次 A 快捷短语（协议 / 大厅 / 备战期 / 门禁） | ✅ ③ 已部署 p22；只差预设表情的贴图素材 |
| 批次 B 服务端（单实例锁 / 连接表 / `/v1/ws`） | ✅ 18 项 pytest，2 次变异 |
| 批次 B 客户端（`RealtimeService.gd` 状态机 + 接线） | ✅ 连接 / 重连退避 / 心跳 / 顶号 / 令牌续期；登录成功后 `start()` |
| 批次 C 服务端（`007_chat.sql` / `chat.py` / `routes/chat.py` / 定时清理） | ✅ 37 项 pytest（不连库）；🔴 **SQL 从没在真库上跑过** |
| 批次 C 客户端（`ChatService` / `ChatScreen` / 主菜单与好友列表入口） | ✅ 出图 `tools/chat_ui_capture.tscn` 验过版面 |
| 批次 D 房间 / 局内自由文字（`ChatText` / 两条 RPC / 大厅与备战期「＋ 打字」/ 顶部输入条） | ✅ 代码完成；联机回归 21 项全过；出图验过最坏情况（24 字昵称 + 40 字）在大厅和备战期都完整显示；🔴 **③ 还没部署 p25** |
| 门禁 `tools/chat_check.tscn` | ✅ 207 项 |

### 🔴 上线前必须按顺序做的（批次 B + C）

1. Supabase SQL Editor 跑 `database/007_chat.sql` —— **必须先于部署 ②**，否则私聊接口 500、
   定时清理每小时报一次错
2. 部署 ②（`sudo bash /opt/glory/src/deploy/update.sh`）。客户端连上时 `journalctl -u glory-backend`
   里应出现 `WS 连接建立` —— 这一条同时证明 Caddy 没掐 WebSocket（第十二节第 3 条）
3. 双号真机验证（同一台电脑开两个客户端是同一个号）：互发、红点、对方在线时即时出现、
   拉黑后对方显示已发送但收不到、删好友后会话消失、断网重连后补齐
4. 部署后定时清理会开始删 30 天前的 `friend_request_log`（005 本来就要求清，一直没人清）

### 🔴 批次 D 上线要做的（与上面 B + C 互不依赖）

1. 打包部署 ③ 到 p25（`make_server_zip.ps1` → `glory_server_p25.zip`，看 `server started protocol=25`），
   **与新客户端一起发** —— 版本不一致时握手直接 `protocol_mismatch`，整个联机都进不去
2. 双号真机：大厅、备战期各发一条 40 字的长消息，看两边都完整显示；4 秒内连发第二条，
   看是否被客户端拦下并提示「发得太快了」（而不是发出去却没显示）；手机上点「＋ 打字」，
   看输入条在键盘上方、打字时看得见

### 下一步从哪继续（按优先级，2026-09-11 重排）

**⓪ 语音**（2026-09-13：游戏内 v1 已写好，见第九节「游戏内语音 v1」）。
下一步：部署 ③ p25 → 出带语音插件的安卓包 → 同事在真实房间里测回声与漏音（步骤与判定见第九节）。
过了再做 Opus、屏蔽某人、战斗界面入口；过不去转商用 SDK。

**① 举报系统**（独立系统，但**上架前必做**；2026-09-11 定：先不做，另起时再排）。资料页「举报该玩家」今天只弹一句「已记录」，
后端什么都没收到。Google Play UGC 政策 / App Store 1.2 要求有玩家内容就必须能举报与拉黑，
且举报要有人处理。做法：新表（举报人、被举报人、原因、**服务器在举报那一刻快照的最近 200 条私聊**、
处理状态）+ `POST /v1/reports` + 资料页接上；审核先在 Supabase 后台直接看表。
会遇到的问题：谁来审、多久回应；恶意举报刷量（按人限频）；快照只能由服务器拍（客户端上传的可伪造）。

**② 核实阿里云内容安全**（2026-09-11 起只挡 E；房间 / 局内 / 私聊都 fail-open，不等它）。核实产品名、接入方式、价格、对中文游戏黑话的误杀率；
然后按第四节做 `app/moderation/` provider（local / aliyun / 测试用假实现），私聊按 fail-open 接进去。
会遇到的问题：每条消息多一次外部调用（100–300ms）与费用；密钥只能放 ② 的 `.env`；
消息内容交给第三方要写进隐私政策。

**③ 批次 E 世界频道**（前置：C + provider）。`world_messages` 表（7 天）、② 进程内 50 条环形缓冲打底、
8 秒 CD、`can_speak_in_world()` 调用点（先返回 True）、拦引流、fail-closed；
UI 做成 `ChatScreen` 的第二个页签。会遇到的问题：广告号（门槛函数到时候要换真实现）、
审核费用随发言量线性涨、单实例的连接数上限。

**④ ~~批次 D 房间 / 局内自由文本~~** ✅ 2026-09-11 代码完成（fail-open、不接 provider），待部署 ③ p25，
见上面「批次 D 上线要做的」。**遗留的设计问题：provider 接通后，房间 / 局内的审核在哪跑。**
文字走 ③ 的 ENet，而 provider 在 ②。三条路：③ 调 ② 的内部审核接口（= 开一条窄的 ②↔③ 通道）、
③ 直接调阿里云（审核逻辑在 GDScript 里再写一份、密钥放上战斗服）、客户端先经 ② 审核拿签名再发给 ③
（③ 验签，要共享密钥）。接 provider 之前要先拍板。举报材料只能由举报方客户端提供（第八节第 5 条）。

**⑤ 批次 F 顶号完整实现**（前置：OAuth 绑定登录）。登录时登记「当前活跃设备」、新设备登录时吊销旧设备的
refresh token、真正的「回登录界面」。今天匿名账号只在一台设备上，触发不了。

**⑥ 其余**：预设表情贴图（批次 A 收尾）；语音 SDK 的一天 timebox 验证；离线消息提醒（没有推送通知，
关着游戏收不到私聊提醒 —— FCM / APNs 是单独一块）；开发用的多账号参数（让一台电脑开两个号测社交功能）；
私聊 SQL 的真库自动化验证（现在只有不连库的静态断言）。

### 还没被回答的产品问题

- 内容审核选阿里云已定，但**接入方式与价格还没核实**，它挡着批次 E（D 已按 fail-open 先上）
- provider 接通后，房间 / 局内的审核放在哪跑（见上面第 ④ 条）
- 国内上线的合规留存要求（第三节「存多少、存多久」末尾）—— 可能要另建合规存档
- 举报由谁审、承诺多久回应
- 语音走哪条路（第九节：TwoVoIP 自建 vs 商用 SDK），以及那一天的验证
- 预设表情的贴图素材

## 十二、待办

0. ~~重新打包部署战斗服务器到 protocol 22~~ ✅ 2026-09-11 已部署（用户确认）。
   确认依据应是 `journalctl -u glory-server` 里的 `server started protocol=22`（过去式那行）。
   🔴 **批次 D（协议 24，与四星撞号后改的）和组队语音（协议 25）都还没部署，一次部署 p25 两件都带上**：`make_server_zip.ps1` → `glory_server_p25.zip`，
   确认依据 `server started protocol=25`。**客户端与 ③ 必须一起发** —— 版本不一致时握手直接
   `protocol_mismatch`，整个联机都进不去，不只是聊天
1. **核实阿里云内容安全的接入方式与价格**（2026-09-11 起只挡 E；D 已按 fail-open 先上）
2. **语音的一天 timebox 验证**（把未知变成已知）—— 2026-09-11 的调研与两条路见第九节
3. **确认 `deploy/Caddyfile` 不会掐 WebSocket 长连接**（上线前必测，本机通不算数）
4. ~~确认 `scenes/prep/PrepScreen.gd` 右下角现有元素~~ ✅ 已做 —— 那里是商店刷新按钮，
   聊天按钮放在它上方，见第六节
5. **战斗阶段（`BattleScreen`）的聊天入口**，以及预设表情的贴图素材
6. 等级制度上线后，替换 `can_speak_in_world()` 的判据实现

### 一条不属于本系统但被它显形的

`docs/账号系统RFC.md` 第三节的**「座位票」仍然未拍板**。本系统四条频道
全部避开了 ②↔③ 通道，所以**不被它挡住、也不推动它** ——
但它自 `C14` 完成后就没有前置了，仍然挂在那里。
