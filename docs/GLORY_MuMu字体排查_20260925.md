# MuMu 字体问题排查（2026-09-25）

## 当前结论与未完成项

用户截图多数汉字正常，异常位置对应房间编号后的全角冒号、状态分隔符和玩家名括号。现有源码已经包含 Noto Sans SC，不能仅凭截图认定为 UTF-8 编码问题或 MuMu 缺少全部中文字体。

本轮按用户指定的“最新源码包 + Mac MuMu”准备验证。MuMu Mac 1.9.2 已下载安装，Apple 公证与网易开发者签名验证通过；官方客户端目前要求登录，CLI 返回 `RemoteWebServerError.invalidUser`，因此**尚未在 MuMu 内安装或运行游戏，未宣称模拟器问题已修复**。已请求用户在本机客户端自行登录，不需要提供密码或验证码。

## 已完成的验证

- 从修复分支 `114e32f87dd3143bc77d78581ad78ab0cf602a49` 构建独立包 `com.glory.stabilityqa`，APK 为 `Glory-debug-20260925-191437-114e32f8.apk`，SHA-256 为 `0786a6ef895af746ca851c708b0d76a3126cc3c0882d88af8f7b95a5d3197d2d`。
- 直接检查 APK ZIP：包含 `NotoSansSC-Regular.otf` 对应导入字体数据（7,596,393 字节）、字体加载脚本与 OFL 许可证。字体不是依赖系统在线下载。
- 扩充 `tools/font_fallback_check.gd`，关闭系统字体回退后验证 556 个字符、52 组混合文本字形排版，包括截图对应的全角标点；测试通过。移除内置回退字体时 `：（）｜` 四个字符均被检测为缺字，恢复后通过；英文排版尺寸不变。
- 将相同产品字体加载脚本、字体、LocaleManager 和测试复制到独立最小 Godot 项目运行，退出码 0，无引擎错误。完整 Android staging 在 Mac 上运行同一测试也通过，但该环境另有 Android 专用语音扩展不支持 Mac 的错误，不能记作全日志无错。
- 在已连接的 Vivo 手机上安装该 APK，进入自定义房间核对：房间编号冒号、玩家名括号、状态分隔符正常。仅创建一个 QA 房间查看界面，未开始战斗，随后通过返回按钮退出并确认回到主菜单。原包名的游戏未替换。
- 手机日志确认运行上述构建；没有字体加载错误。另有 Effekseer 动态库 ELF 头错误和 glory_voice 缺少 Android arm64 库错误，属于独立扩展问题，不作为字体乱码根因，也未隐藏。

本轮没有在未复现的情况下更换字体、改写玩家昵称、替换全角标点或改变 UI 风格；新增的是字体排版回归覆盖。用户原截图对应的旧 APK 未提供，无法对原包的字体内容作直接溯源。

## MuMu 登录后的验证步骤

1. 通过 MuMu 自带 CLI 获取 Android 实例和 ADB 地址，安装上述有明确哈希的 APK。
2. 抓取该应用进程的 logcat，核对 `GLORY_BUILD`、字体加载、渲染器及扩展错误。
3. 进入自定义房间，比较截图中 `：`、`｜`、`（我）` 和主要中文控件；检查重新启动后是否一致。
4. 若最新 APK 仍异常，按实际缺字、字体缓存或渲染器证据修改，重新构建后在相同模拟器验证。若不复现，明确记录“最新包未复现”，不把无法溯源的旧图当作已证实的引擎故障。

本地未提交原始证据位于 `reports/font-mumu-20260925/`：`apk-font-inventory.json`、`font-check-isolated.log`、`phone-room.png`、`phone-after-leave.png` 及应用日志。日志仅保存在本地。
