# 9.19 第二批记录：9 条新战斗音效接入 + 剑士技能冷却文案

> 日期：2026-09-19
> 素材来源：`C:\Users\WINDOWS\Desktop\音乐\0919\战斗、特效\`（9 个文件）
> bug 来源：`C:\Users\WINDOWS\Desktop\9.19bug提交及修复.docx` 第 1 条
> 范围：① 9 个新素材落盘 + 接线；② 两条硬约束的门控实现；③ 剑士技能文案补冷却；
> ④ 顺手修掉 D6 迁移漏字段导致的一处既有缺陷。

---

## 一、用户给的两条硬约束

原话：

> 一、以上战斗音效都只有自身的棋子才会播放。
> 二、人王战斗结束未阵亡奖励属性，要满足两个条件才会播放：1、本次战斗自身人王未阵亡；
> 2、本次战斗自身人王可以触发技能，有属性加成，而不是已达到上限。

二次确认（问答）后补充的口径：

- 「四星」前缀**不是合成时刻**，而是**该棋子四星时释放技能 / 攻击**才播；
- 弓箭手 / 牧师 / 极光射手这类**攻击触发**的技能，在那一下真的触发时才播；
- 人王奖励音**不放战后结算面板**，要在**战斗结束那一刻**、在自身人王身上出一个
  **只有自己看得到**的闪光升级特效，并伴随该音效。

---

## 二、素材落盘

一律英文 snake_case，落在 `assets/audio/sfx/battle/`（战斗音效目录）。

| 源文件名 | 工程路径 | 归属 |
|---|---|---|
| 人王战斗结束未阵亡奖励属性.mp3 | `battle/human_king_reward.mp3` | 人王战后奖励 |
| 四星剑士技能.mp3 | `battle/star4_swordsman_skill.mp3` | `human_swordsman` 盾击（施法型） |
| 四星弓箭手技能触发额外伤害.wav | `battle/star4_archer_skill.wav` | `human_archer` 第 N 击额外伤害（攻击型） |
| 四星极光射手攻击.mp3 | `battle/star4_aurora_skill.mp3` | `god_aurora` 每次普攻真伤（攻击型） |
| 四星法师技能.mp3 | `battle/star4_mage_skill.mp3` | `human_mage` 随机法术（施法型） |
| 四星牧师技能触发治疗.mp3 | `battle/star4_cleric_skill.mp3` | `human_cleric` 第 N 击治疗（攻击型） |
| 四星神侍、四星天使技能释放.mp3 | `battle/star4_priest_skill.mp3` | `god_priest` + `god_angel`（共用一条，用户命名如此） |
| 星轨猎人技能.wav | `battle/merc_arrow_rain_skill.wav` | `merc_sagittarius_rain` |
| 泡沫术士、圣愈修女技能.mp3 | `battle/merc_bubble_holy_song_skill.mp3` | `merc_pisces_bubble` + `merc_virgo_heal` |

**格式核查（踩过坑，必须查）**：9 个文件的头 4 字节分别是 `49 44 33 04`（`ID3\x04`，
ID3v2.4 的 MP3）与 `52 49 46 46`（`RIFF`/WAVE）—— 都是真格式。9.17 那批里出现过
「扩展名 `.mp3`、实际是 M4A」的文件，导入阶段不报错、`load()` 才炸，所以这一条每次都查。

**落盘后另一个探针实测**（`load()` + `get_length()`）：9 个全部解码成功，
时长 0.295s ~ 2.0s，无零长度、无解码失败。

### 素材内容重复（已在交接里向用户说明）

9 个文件的 sha256 只有 **6 个唯一值**：

| 重复组 | 文件 |
|---|---|
| ×2 | `star4_archer_skill.wav` == `merc_arrow_rain_skill.wav`（180180B，逐字节相同） |
| ×3 | `star4_cleric_skill.mp3` == `star4_priest_skill.mp3` == `merc_bubble_holy_song_skill.mp3`（17180B，逐字节相同） |

另有 5 个文件恰好同为 17180B（编码时长相同的巧合，内容不同）。
**按用户命名原样落盘，未做任何替换** —— 这是素材本身的取舍，不是接线问题。
如果要各家不同音色，需要用户重新提供素材。

---

## 三、逐条接线

### 3.1 按「怎么触发」分三张表（`ui/services/SfxService.gd`）

`STAR4_SKILL_CUES` 原是「四星技能音」的唯一表，但它的调用点是 `skill_ready` 的**上升沿**——
只覆盖「施法型」技能。「第 N 次普攻触发」的三家**根本没有 skill_ready 边沿可挂**
（见 `BattleSimulator._step_team:636/640` 与 `_perform_attack:697`）。所以拆成三张表：

| 表 | cue | 派发入口 | 门控 |
|---|---|---|---|
| `STAR4_SKILL_CUES`（扩了 4 家） | 剑士 / 法师 / 神侍 / 天使 | `star4_cue_for(uid, true)` | 自身 **且** 四星 |
| `STAR4_ATTACK_SKILL_CUES`（新增） | 弓箭手 / 牧师 / 极光射手 | `attack_skill_cue_for(uid)` | 自身 **且** 四星 **且** 这次真触发 |
| `MERC_SKILL_CUES`（新增） | 星轨猎人 / 泡沫术士 / 圣愈修女 | `merc_skill_cue_for(uid)` | 只判自身 |

**为什么佣兵必须单独成表**：佣兵**升不到四星**（`EconomyLedger._use_upgrade_stone` 会拒），
把它们并进 `STAR4_SKILL_CUES` 就等于要求调用方再叠一个永远为假的 `star == 4`，
结果是一条永远不响的音。

新增 9 条 cue 常量 + 9 条 `CUES` 登记，`EXPECTED_CUE_COUNT` **37 → 46**。

### 3.2 「自身棋子（不含队友）」怎么判

沿用 9.18 那套：`BattleVfx._is_local_owned_unit(sim_uid)` —— 判据是
`owner_slot == NetworkService.team_local_slot`（非组队时退化成
`team == "player" and owner <= 0`）。队友是**别的 owner_slot**，所以天然被排除，
不需要额外写「排除队友」；敌方更不用说。

### 3.3 「四星 **且** 这次真的触发」

- **施法型**：`_play_skill_cast_vfx` 本来就在 `skill_ready` 上升沿调用，
  原有的 `_is_local_owned_unit() and _is_star4()` 门控直接复用（人王 / 神王 / 母灵 / 黑龙 /
  大天使 / 末日守卫六家早就在这条路上），四家新棋子只是往表里加了映射。
- **攻击型**：挂在 `_play_attack_unit_procedural` 里、`_attack_skill_vfx_ready()` **通过之后**。
  那一步判的就是 `attack_count % every == 0`，所以「走进来」本身就等于「这一次真的触发了」。
- **佣兵**：`_play_skill_cast_vfx` 里新加一条 `elif _owned:` 分支，只判自身。

### 3.4 人王「战斗结束未阵亡 · 奖励属性」

`BattleVfx._play_human_king_reward()`，由两条收尾路径调用：

- 本地模拟：`BattleResult._emit_finished()`
- replay / 组队：`BattleScreen._finish_replay()`

两条都在 `_return_emitted` 之后、**水晶演出之前**调用，`_play_human_king_reward` 里再用
`_human_king_reward_played` 二次去重（每局一次，`BattleScreen._start_replay()` 清空）。

**为什么必须在水晶演出之前**：赢的那一方棋子会被 `_vanish_crystal_attacker()` 逐个淡出并
`queue_free()` —— 挂在水晶演出之后就**没有人王可以闪光了**。

三个条件全部满足才响（缺一不可）：

| 条件 | 判据 | 对应 `Main` 侧的同源逻辑 |
|---|---|---|
| 自身人王 | `_is_local_owned_unit(uid)` | `result.player_survivor_slots` |
| 本场未阵亡 | `_state` 里该 uid `alive == true` | 同上 |
| 还有成长空间 | `king_growth_stacks < max_stacks` | `Main._grow_human_king` 的 `cap` 判定 |

**上限一律经 `UnitFactory.apply_star_stats(d, star)` 取**，与 `Main._grow_human_king` 同一条路：
`max_stacks` 在 `star4` 里被覆写（★1~3 = 5 层 / ★4 = 8 层），而 `cell.def` 是**未做星级缩放的
原始表项** —— 直接读它会把 4 星的上限读成 5 层，于是人王明明已经封顶了还在响奖励音。

**闪光特效**：3D 程序化 `GROWTH_AURA`（绿色光环 + 上升粒子，就是「成长」那条特效）
叠一层同 id 的 2D 光环，落在**人王本体**的 `world_foot` / `foot_pos` 上，
不是队伍中心 —— 用户要的是「在自身人王身上」。

---

## 四、顺手修的既有缺陷（本轮发现）

### 4.1 cue 路径漏了 `attack_count` / `skill_every`（D6 迁移遗留）

`_attack_skill_vfx_ready()` 用 `attack.get("attack_count")` 与 `attack.get("skill_every")`
判断「这次普攻是不是技能触发那一击」。D6 把老的 snapshot-diff 路径换成 cue 路径时，
`cue_play_basic_attack()` 拼的 `attack` 字典**漏了这两个字段**，
于是每次调用都读到缺省值（`attack_count = 0`、`every = 4`）→ `0 % 4 == 0` → **恒为真**。

后果是弓箭手 / 牧师的技能演出**每一次普攻都在放**，而不是「每第 N 次」。
（`_collect_vfx_units` 一直在写这两个字段，只是没人读 —— 属于「数据有、消费方漏读」。）

修法：在 `cue_play_basic_attack()` 里从快照取回这两个字段。
`every` 只在 > 0 时才写进字典，否则让 `_attack_skill_vfx_ready()` 走它原本的缺省，
1~3 星与非该类技能的行为与改动前逐字一致。

> **不修它就没法接音效**：四星弓箭手是「每 3 击」（★1~3 是每 4 击），
> 这个数字只有快照里的 `def.every` 知道。

### 4.2 门禁 `indirect_cue_entry_unused` 的 `contains()` 能被前缀骗过

`audio_sfx_check._check_every_cue_has_call_site()` 用
`corpus.contains("SfxService." + entry)` 检查派发入口有没有生产调用点。
变异测试实测：把调用改名成 `attack_skill_cue_for_MUTATED` 时，
**不带 `(` 的 `contains()` 依然命中**（那是它的前缀），门禁照旧全绿。

已收紧为 `corpus.contains("SfxService." + entry + "(")`（要求的是**真调用**，不是「出现过这个名字」），
并把这行变异写进注释。收紧后同样的变异正确变红。

---

## 五、bug 文档第 1 条：剑士技能未显示冷却时间

原文：

> 1、剑士技能未显示冷却时间，在「盾击」后面增加冷却时间描述，「盾击（冷却5.0秒）：」

**根因**：`BattleSimulator._tick_skills` 里剑士的技能是

```gdscript
"front_cone_stun":
    BattleSimSkills._skill_front_cone_stun(caster, opponents, d, state)
    caster.skill_ready = float(state.elapsed) + float(d.get("skill_cd", 5.0))   # ← 缺省 5.0
```

但 `data/units/race_units.json` 里 `human_swordsman` **根本不带 `skill_cd` 字段**，
而 `UnitDetailFormat.skill_cd_text()` 只在 `d.has("skill_cd")` 时才吐「（冷却X秒）」
→ 文案少了一整句。原来的 `black_hole` 是同一个问题的特例（硬编码了一句 8.0 秒）。

**修法**：把「数据不带、但实现确实有冷却」的技能登记成一张表，数值与模拟器缺省值同源：

```gdscript
const IMPLICIT_SKILL_CD := {
    "black_hole": 8.0,
    "front_cone_stun": 5.0,
}
```

`black_hole` 从硬编码分支改为走这张表，输出与改动前**逐字相同**；
`front_cone_stun` 现在渲染成：

```text
盾击（冷却5.0秒）：攻击最近敌人，造成自身攻击150%伤害并眩晕1.0秒。   （★1）
盾击（冷却5.0秒）：攻击最近敌人，造成自身攻击300%伤害并眩晕2.0秒。   （★4）
```

中英文两支都跟着走（英文是 `Shield Bash (CD 5.0s): ...`）。

---

## 六、验证

### 6.1 行为级探针 `work/_qa_919/probe_battle_sfx_919.tscn`（**34 项，全过**）

headless 下不 instantiate 重型 3D 场景，只 `preload` 目标脚本、`BattleVfx.new()`（**不进树** ——
它的继承链 `BattleVfx→BattleRenderer→BattleArena→BattleUI` 的 `_ready()` 装配在 headless 里
代价极大），手工喂 `_state` / `_vfx_prev_units`，再数 `SfxService.play_count(cue)` 的真实次数。

覆盖：

- 攻击型 11 项：第 4 击响 / 第 5 击不响 / 3★ 不响 / 敌方不响 / 极光每次触发 / 牧师四星 every=4 / 无人王素材不响；
- 佣兵 5 项：星轨猎人 1★ 响、泡沫术士 3★ 响、圣愈修女响、敌方不响、非佣兵不吃佣兵音；
- 四星施法 5 项：剑士 4★ 响 / 3★ 不响 / 法师 4★ 响 / 神侍与天使各响 / 敌方 4★ 不响；
- 人王奖励 7 项：存活+未满层响 / 已满层不响 / 阵亡不响 / 敌方不响 / 棋盘无人王不响 /
  重复调用只响一次 / 4★ 在 5 层（未到 8 上限）仍响；
- 剑士文案 5 项：★1、★4 均以「盾击（冷却5.0秒）：」开头，且 `black_hole`、
  带 `skill_cd` 的、无冷却的三条既有分支未被改坏。

**探针自身踩到的三个坑（都记在脚本注释里）**：

1. `_play_skill_cast_vfx` 第 4 个参数是 `Array[Dictionary]`，传 `[]` 会报
   「does not have the same element type」而**整条调用被拒** —— 断言于是静默地什么都没验。
   必须传类型化空数组。
2. `SfxService` 的 **40ms `RETRIGGER_GUARD_MSEC`**：神侍与天使共用同一条 cue，
   同一帧连调两次第二次必然被挡掉。凡是「同一条 cue 响 N 次」的断言，两次之间必须
   `reset_counters_for_check()` 或间隔 > 40ms。
3. 「每场只响一次」这类断言必须先清掉上一用例留下的每场一次标记，
   否则用例①置的 `true` 会让用例⑥静默拿到 0。
4. 脚本**解析期**报错时场景里的 Node 拿不到脚本 → `quit()` 永不执行 → 进程不报错也不退出，
   表现为「挂死」（实测 5 分钟无输出）。所谓「重型场景不能 instantiate」的观感里，
   至少有一次其实只是解析错误。

### 6.2 门禁（11 条，全 PASS）

| 门禁 | 结果 |
|---|---|
| `audio_sfx_check` | PASS（163 项）—— cue 数 37→46、新增 8 条间接 cue 与 3 个派发入口 |
| `synergy_activation_sfx_check` | PASS |
| `team_merc_summon_sfx_check` | PASS |
| `prep_mute_state_check` | PASS |
| `battle_presentation_director_check` | PASS |
| `battle_presentation_event_check` | PASS |
| `battle_card_check` | PASS |
| `prep_detail_overlay_check` | PASS |
| `battle_hit_victory_check` | PASS |
| `unit_name_consistency_check` | PASS |
| `four_star_values_check` | PASS |

### 6.3 变异测试（4 处改坏，全部正确变红）

| 变异 | 期望 | 实测 |
|---|---|---|
| `EXPECTED_CUE_COUNT` 46 → 45 | `cue_count_changed` 红 | ✅ 红 |
| 攻击型派发入口调用改名（不含 `(` 的 `contains` 版本） | —— | ❌ **仍绿**（见 §4.2，已收紧匹配串） |
| 收紧后：调用换成 `merc_skill_cue_for` | `indirect_cue_entry_unused` 红 | ✅ 红 |
| `front_cone_stun` 的缺省冷却 5.0 → 3.0 | 剑士文案断言红 | ✅ 红（输出变成「冷却3.0秒」） |

4 处变异全部还原到**与验证时逐字节相同**（sha256 见 §七）。

### 6.4 计数型门禁的归属对照（本轮改动与它们无关）

| 门禁 | 读数 | 结论 |
|---|---|---|
| `dynamic_call`（`unresolved_grew`） | **265**，上限 191（既存红） | 抽掉本轮新增的 `_UnitFactoryRef.apply_star_stats()` 调用 → 仍 265；抽掉两处 `_play_human_king_reward()` 调用 → 仍 265。**本轮贡献 0**。（交接里此前的记录是 263，那是更早批次的读数，之后被别的批次推到了 265。） |
| `asset_manifest`（`missing_asset`） | **21**（既存红） | 21 条全部是 `_qa_*` / `work/bug09*` / `棋子属性表生成_20260911/` 那批老探针引用了已删文件；**没有一条指向本轮的 `_qa_919`**。本轮新增的探针文件所引用的资源全部存在。 |

> 两个数都不是本轮引入的；探针放在 `work/_qa_919/`（`dynamic_call` 不扫 `work/`），
> 保留下来给后续批次复跑。

---

## 七、交付与清单

- 改动文件 6 个（sha256，与门禁/探针验证时逐字节相同）：

```text
9b732672622af8d519af386785d4886065c9b643d8479af9b212391ce2719ead  ui/services/SfxService.gd
5d12a322aa954ae75267210292a708669e860e8d4d5d32cb489dd0d443b97f86  scenes/battle/BattleVfx.gd
8cbea26ab92f6e8b1aafcfd5a2826dcf80af021c4c81b072600aaee66b49ef46  scenes/battle/BattleScreen.gd
923ae8dfe47bc3b5d86cba960a8ee29ee669734c39f3c0f81d61c09542e1f232  scenes/battle/BattleResult.gd
049a62759a3c4e3aa70b4aaf3530911d912faae77bb4120e785b63ffc93aa18e  scripts/ui/UnitDetailFormat.gd
462b1cf8354ce9e5448f38bb1285f4a3870447ea00d643f50c1b7eb4484f3b67  tools/audio_sfx_check.gd
```

- 新增音频 9 个 + 9 个 `.import`（`assets/audio/sfx/battle/`），已 `--import` 重导。

### 未做 / 已知项

- **未做真机 / 安卓听感验收**：headless 是 Dummy 音频驱动，只能验「登记对、文件在、能解码、
  门控对」，验不了音色与音量。
- **`assets.bundle.json` 未更新**（9.17 起的老遗留）：`assets/` 是外置资源，
  交付走 `tools/package_assets.ps1`；下次重打包这些音频自然进包。
- **许可**：`assets/audio/` 整组仍登记 `awaiting_user_records`；AI 生成素材需给出
  「生成工具名 + 当时 ToS 快照」。
- **素材内容重复**见 §二（弓箭手 == 星轨猎人；牧师 == 神侍·天使 == 泡沫术士·圣愈修女）。
### 交付夹（桌面，同路径覆盖用）

上一批的「两个最终覆盖上传夹」在本轮开始时**已不在桌面**，无法确认是否已上传，
所以本轮按**累积口径**重建 —— 覆盖范围 = 01:44（本轮修改前）以来的全部音效相关改动
（9.18 批 + 9.19 第一批 + 本批），夹内 `_说明.txt` 按批次逐条标注、
并写明「若上一批已上传可跳过」的 4 个 9.18 代码文件。

| 夹 | 文件数 | 内容 |
|---|---|---|
| `桌面\最终覆盖上传_资源\` | **44** | 22 条音频（7 + 6 + 9）+ 22 个同名 `.import`，全在 `assets/audio/sfx/` 下 |
| `桌面\最终覆盖上传_代码\` | **15** | 11 个 `.gd`（4 + 1 + 6）+ 4 个文档（3 篇记录 + `README.md`） |

各带 `_说明.txt`（noBOM / LF）与 `_sha256清单.txt`（BOM / CRLF），**59 个文件逐个复核与现工程
逐字节相同**，清单每行的 sha256 与该文件当前值一致（0 错）。
归属核对用 `GLory-codex` 镜像做旁证：`codex` 正好停在上一批交付态（`SfxService` = `c888d8ed…`
33302B、`BattleVfx` = `b915fce4…` 70012B），与工程比对后**内容不同的恰好是本轮 6 个代码文件**，
「仅 codex 没有」的 18 个音频 + 本批记录文档也一一对上，没有漏项、没有多塞。

### README 里那段没解开的 git 冲突块（本轮顺手清掉）

`README.md` 原本卡着 `<<<<<<< Updated upstream`（1287 行）/ `=======`（1313 行）/
`>>>>>>> Stashed changes`（1411 行）—— 是一次 stash 冲突没解干净留下的，整个文件尾部都在冲突块里。
两侧内容**互不重叠**（上游侧 = 9.18测试反馈修复 + 9.19四星分离；暂存侧 = 9.19 语音 LiveKit
第 1 / 2 阶段 + p31 发布包），所以按「只删三行标记、两侧全保留、按时间顺序拼接」处理，
改完冲突标记 0 处、五节标题各仍在 1 处；改动前的 README 已备份到
`%TEMP%\README.before_919b.md`。**如果本来只想留其中一侧，需要用户拍板再改。**
