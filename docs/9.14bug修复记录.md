# 9.14 bug 修复记录（2026-09-14）

项目：GLory-work。依据桌面《9.14bug提交及修复.docx》，原问题文档保留不变。

本轮共处理 **11 条**反馈：7 条棋子缺陷（巨甲灵、母灵、末日守卫、民兵、死侍、法师、人王）、4 条功能/UI（离线自测实时数值、自定义界面、朋友列表、查看另一队）。

## 问题、修改结果与检验方法

| # | 问题 | 修改结果 | 检验方法 |
| --- | --- | --- | --- |
| 1 | 巨甲灵：毒甲文案「自身防御每次+x%，最多10层」，离线实测防御加成无封顶 | `scripts/battle/BattleSimTreasures.gd` 的毒甲分支只把 `skill_stacks` 用 `mini(max_stacks)` 封了，`target.defense` 却每次受击都无条件加 —— 第 11 层起防御继续涨。现在把「加防」放进「层数确实涨了」的分支（与 9.13 #8 裁决者同源的毛病：**计数器封顶 ≠ 效果封顶**） | 探针让 4★ 巨甲灵连续受击 16 次：基础 def 72、每次 +20（= 自身 4★ def 的 28%），满 10 层时 def=272，再打 5 次仍为 272 → **已封顶**。裁决者同探针：满 5 层 def=86，再打 5 次仍 86 → 已封顶 |
| 2 | 母灵：1-3星三阶即死实测 15%，文案写 10%；4星要改成 1阶/佣兵55%、2阶35%、3阶10%、Boss 30%、灵7 每 3 死触发 | 两个根因一起修：① `BattleSimTreasures._mother_execute_on` 原来把三档当**累积带**算（`roll<t1→1，<t1+t2→2，其余→3`），数据表里的 `tier3_chance` 是从未被读取的死字段，于是 1-3★ 史诗档恒为 `1-0.50-0.35 = 15%` —— 现在三档各自读数据表，落在 `t1+t2+t3` 之外的那段是设计文档 §3 的「空档」（1-3★ 合计 95%，触发但不处决；4★ 合计 100%）；② `data/units/race_units.json` 里 4★ 的 `tier2_chance` 由 0.30 改回 0.35 | 探针实跑 `_mother_execute_on` 各 20000 次：1★ = 52.3% / 37.4% / 10.3%（另 1044 次无目标 = 5% 空档），4★ = 54.5% / 35.6% / 9.8%（无空档）；数死亡计数：1★ 5 死、灵7 4 死；4★ 4 死、灵7 3 死；对 Boss（10000 maxHP）扣血 1★ 2000（20%）、4★ 3000（30%）；1★/4★ 文案与之一致 |
| 3 | 末日守卫：4星连接期间每秒回 3% 最大生命，文案没写；另需「连接目标不能是末日守卫」的隐藏机制 | ① `scripts/ui/UnitDetailFormat.gd` 血链文案读 `link_regen_pct`，>0 时才追加「连接期间每秒回3%最大生命」（该字段只在 4★ 的 `star4` 里，1-3★ 文案自然不带该句）；② `BattleSimulator._skill_shared_hp_link` 的候选池改用新增的 `_link_targets_without_doom()`，剔除 `shared_hp_link` 单位（含 `id=dark_doom`），Boss 仍由 `_nearest_non_boss` 拦 | 文案对照：1★ 无回血句、4★ 有；代码路径 `_skill_shared_hp_link → _link_targets_without_doom` 已接线（候选池剔末日守卫）。**未做**专项对局探针 |
| 4 | 民兵：4星实测 20% 概率眩晕 0.5 秒，与文案（缴械）不一致 | `attack_interrupt` 文案按 `stun_sec` 分两支：有 `stun_sec`（只写在 4★ 的 `star4`）就输出「20%概率眩晕目标0.5秒」，否则维持 1-3★ 的「12%概率缴械目标（1 秒内无法普攻）」。文案与 `BattleSimulator` 的实现同源 | 探针实跑各 200 次普攻：1★ stun=0 / interrupt=22（11%），4★ stun=38 / interrupt=0（19%，时长恒为 0.5s） |
| 5 | 死侍：4星加防效果未生效；且 4星可绑左右两枚棋子（设计 +30% 防御、持续 8 秒），文案只写绑左侧 | ① 根因是 `BattleSimSkills._apply_death_servant_aura` 写 `float(a.get("def", 0))` —— fighter 字典里的 `def` 是**单位 def 子字典**而不是数字，GDScript 抛 `Invalid call. Nonexistent 'float' constructor.` **当场中断整个函数**，连第一个友军都还没加防就退出；只有 4★ 的百分比支路走这行，3★ 走绝对值支路，所以表现为「3★ 正常、4★ 全无」。改为读 `a.get("defense", 0)`。② 文案读 `ally_def_pct`：4★ 输出「周围一圈友军防御+30%持续8.0秒；…绑定左右两侧棋子」 | officetest 真实组装路径探针：3★ 死侍 2 个邻军各挂 `amount=3 remaining=5.0`（面板累计 10.0s），4★ 各挂 `amount=2 remaining=8.0`（累计 16.0s，amount = 基础 5 × 30%）；4★ 仅左邻 / 仅右邻两种布局各累计 8.0s → **左右两侧都绑** |
| 6 | 法师：实际存在普攻，文案写「无普攻」；4星实测冷却 2s、600%、20% 双触发，与文案不符 | ① `data/units/race_units.json` 法师加 `no_basic_attack: true`；`BattleSimulator._step_team` 在攻击分支前判该标记直接 `continue`（移动分支照常，法师仍会走位、被集火、被技能选中，输出全交给 `random_attribute_bolt`）。② 文案读 `skill_cd` / `damage_atk_pct` / `double_element_chance`：1-3★「每2.5秒…攻击300%」，4★「每2.0秒…攻击600%…并有20%概率触发双重施法」 | 探针实跑 4★ 法师技能伤害 = 1488 = atk 248 × 6（600%）；文案与数据一致。**未做**普攻次数长局统计 |
| 7 | 人王：1-3星上限 5 层、4星上限 8 层，文本未写；4星实测成长倍率只有 1.2，应为 1.3 | ① `data/units/race_units.json` 里 4★ 的 `post_battle_all_stat_growth` 由 0.25 改成 0.30（×1.3）。② `scenes/main/Main.gd._grow_human_king` 原来只把**上限**改走 `UnitFactory.apply_star_stats`，**倍率**仍读棋盘格的原始 `d`，于是 4★ 出现「上限是 4★ 的 8 层、倍率还是 1-3★ 的 ×1.2」这种半接线状态；现在两者都读 `effective`。③ 文案读 `max_stacks`，输出「全属性永久×1.2（上限5层）」/「…×1.3（上限8层）」 | 探针跑 12 轮：1/2/3★ 每轮 ×1.20、停手于 5 层；4★ 每轮 **×1.30**、停手于 8 层，封顶后 hp/atk/def 不再变；与「对照组（`def` 先过一次 `apply_star_stats`）」结果一致 |
| 8 | 离线自测需要一个「实时数值展示」：战斗开始后鼠标悬停棋子就显示该棋子的实时数值面板 | `BattleRenderer._make_unit_node` 给每个单位挂 `mouse_entered` / `mouse_exited` → 新增基类空回调 `_on_battle_unit_hover(unit_id, entered)`（正式对局不受影响）。`officetest/OfficeTestScreen.gd` 覆写它：演示态开一个左上角 `MOUSE_FILTER_IGNORE` 面板（不挡鼠标），内容取父类每帧重建的 `_frame_fighter_by_id` 快照 —— 名称★级、生命/护盾、攻击/攻速、防御/暴击、射程/技能层数、攻击次数/已造成伤害、技能冷却、状态及剩余秒数；`_process` 里跟当前帧刷新，棋子本帧阵亡即收起，回编辑态清空 | officetest 冒烟：进演示态 → 悬停出面板、移开收起、阵亡收起、回编辑态隐藏；无引擎错误 |
| 9 | 优化自定义界面：不再显示 Token ID / 输入Token ID / 生成 / 恢复；把「创建房间」上调到与第一个房间对齐，「输入房间ID」「加入房间」同步上调，「关闭」位置不变 | `scenes/menu/MainMenu.gd._build_room_panel()` 移除 `Token ID` 标签、`输入Token ID` 输入框与 `生成` / `恢复` 两个按钮（网络侧动作与 Main 的信号处理保持不动 —— `tools/main_team_token_action_check` 是直接调 Main 的处理函数、不经过这些控件，仍通过）。移除后左栏首项就是「创建房间」，与右栏第一个房间行自然对齐；标题给 42 的最小高度与右栏表头等高，保证对齐；「关闭」前加一个可伸缩空白把它继续压到底部（位置不变） | `main_team_token_action` / `main_team_create_room_action` / `main_team_join_room_action` / `main_team_room_list_action` / `main_team_short_code_resume_action` 五项通过（均在 FakeMenu 上跑，与面板结构解耦） |
| 10 | 调整房间内「朋友列表」里朋友 ID 的位置，使其在框框里边向左对齐 | `scenes/menu/Team3v3Lobby.gd`：列表项显式 `horizontal_alignment = LEFT` + `size_flags_horizontal = EXPAND_FILL`；并把滚动区从 x=1350（宽 210）移到 x=1374（宽 165）—— 羊皮纸木框的内边约在 1373，原来文字压在木框上，看着「没在框里」，两处一起改才看得出来 | `friends08` / `friends` / `lobby050607` / `lobby_identity03` 四项通过；像素探针确认框内区域（右边界 1539 不变） |
| 11 | 对局中战斗场景里「查看另一队」与对方视角显示不同步、存在错误显示 | `BattleRenderer._display_team` 与 `BattleArena` 的半场几何 / 段签名都去掉了 `_watching_rival` 参与的反转 —— 原来观战另一队会把对方 replay 的 "player" 侧染红、半场标注翻面，导致同一场战斗在两台设备上配色与半场相反。现在只保留 `_arena_flip_y`（那是「我方永远在下方」的本地镜像，与观战无关），「查看另一队」呈现的就是那位玩家自己屏幕上的画面 | `replay_identity_split` / `replay_transfer` / `battle_presentation_director` / `board_readability` 四项通过 |

## 修复后探针实测（关键读数）

探针跑在 `res://_qa_star4/*.tscn`，走的是产品真函数（`BattleSimTreasures._apply_defender_reaction`、`_mother_execute_on`、`BattleSimSkills._apply_death_servant_aura`、`Main._grow_human_king`、`OfficeTestSim.build_test_state` 等），不是复刻逻辑。

- **巨甲灵 / 裁决者（层数封顶）**：巨甲灵 4★ 基础 def 72、每次 +20（28%），满 10 层 def=272，再打 5 次仍 272 → 已封顶；裁决者 4★ 满 5 层 def=86，再打 5 次仍 86 → 已封顶。
- **母灵（档位与阈值）**：1★ 52.3/37.4/10.3%（+5% 空档）、4★ 54.5/35.6/9.8%；触发阈值 1★ 5 死 / 灵7 4 死，4★ 4 死 / 灵7 3 死；Boss 扣血 20% / 30%。
- **民兵**：1★ interrupt 11%；4★ stun 19%、时长恒 0.5s。
- **死侍**：3★ 邻军各 +3 防 / 5s（面板累计 10s），4★ 邻军各 +2 防 / 8s（累计 16s）；4★ 仅左邻或仅右邻均累计 8s → 左右都绑。
- **法师**：4★ 技能伤害 1488 = atk 248 × 6。
- **人王**：1-3★ 每轮 ×1.20、5 层封顶；4★ 每轮 ×1.30、8 层封顶，封顶后不再增长。
- **全量 star4 摊平**：`S4TOTAL units_with_star4=30 keys=70 failures=0`（1★ 文案与 4★ 文案逐条对照见下）。
- **1★/4★ 文案抽查**（`UnitDetailFormat`，两者自动显示各自那一份）：
  - 血链：1★「…双方共享受到的生命损失。」／4★「…；连接期间每秒回3%最大生命。」
  - 毒甲：1★「每次+14%，最多10层」／4★「每次+28%，最多10层」
  - 母灵：1★「每5个…50%、35%、10%，Boss 20%，灵7 每4个」／4★「每4个…55%、35%、10%，Boss 30%，灵7 每3个」
  - 缴械攻击：1★「12%概率缴械目标（1秒内无法普攻）」／4★「20%概率眩晕目标0.5秒」
  - 随机法术：1★「每2.5秒…300%」／4★「每2.0秒…600%…20%概率触发双重施法」
  - 死侍契约：1★「+3持续5.0秒…绑定左侧棋子」／4★「+30%持续8.0秒…绑定左右两侧棋子」
  - 人王：1★「×1.2（上限5层）」／4★「×1.3（上限8层）」

## 本轮对测试夹具（tools/）的改动

`tools/four_star_values_check.gd` 第 11 条 `_case_execute_epic_band_not_reduced`：原断言按旧的「史诗档 = 1 − t1 − t2」余数语义计算，母灵（#2）改成显式三档后这个前提不再成立（旧式会算出 4★ 10% < 1-3★ 15% 而报红）。夹具改为直接读 `tier3_chance`，并守住两件事：4★ 的史诗档不低于 1-3★；三档之和不超过 100%（否则最后一段被压成负长度）。**只改夹具口径，未放宽断言强度。**

## 验证

- **相关专项 30 项**（`work/_qa_913/run_checks.py` 同款批量跑测）：

  | 轮次 | 结果 |
  | --- | --- |
  | 首跑 | 26 通过 / 4 失败 |
  | 复跑 | **29 通过 / 1 失败** |

  首跑的 4 项逐一交代：
  1. `four_star_values` —— 首跑用的是**旧夹具**（上节那一条）；按新契约更新夹具后复跑 **PASS**（`checked=683 failures=0`）。
  2. `procedural_ui_ratchet` —— **既存红**。命中 `scenes/prep/FourStarUpgradePanel.gd`（本轮**未改动**，work 与 `GLory-codex` 逐字节一致）自绘调用 `StyleBoxFlat.new() 0→1`、`Button.new() 0→3`，基线未收紧。在 codex 镜像（= 9.13 状态）复跑**同样红**，与本批无关。
  3. `main_team_create_room_action` / 4. `main_team_join_room_action` —— 首跑红，但**单独复跑通过、在 codex 镜像通过、复跑整批也通过**。每项检查都是独立 Godot 进程，故不是跨用例污染；判定为**批跑下的偶发（顺序/环境敏感）**，与本批无关（本轮唯一改到 `Main.gd` 的是一行 `_grow_human_king` 的倍率，与房间动作无关）。
- 整批通过清单（29 项）：`cold_parse_chain`、`four_star_values`、`four_star_upgrade`、`unit_name_consistency`、`battle_presentation_director`、`replay_identity_split`、`replay_transfer`、`board_readability`、`determinism`、`main_team_token_action`、`main_team_create_room_action`、`main_team_join_room_action`、`main_team_room_list_action`、`main_team_short_code_resume_action`、`friends08`、`friends`、`lobby050607`、`lobby_identity03`、`ui_component`、`modal_lifecycle`、`ui_feedback`、`bug0912`、`bug0911`、`bug_0908_regression`、`bug_0909_regression`、`treasure_bug0910`、`startup_ui_regression`、`responsive_layout`、`main_screen_scenes`。
- 冷解析链 `cold_parse_chain_check` 通过；整仓 headless 启动零解析/编译错误。
- **未做**：安卓真机、真实多客户端 / 旧服务端联机实测；未导出 EXE / APK；未部署服务器；未刷新资源清单基线（原因同 9.13）。

## 已知的既存红项（非本轮引入，未处理）

- `procedural_ui_ratchet_check`：`scenes/prep/FourStarUpgradePanel.gd` 的自绘调用未登记进 `data/qa/procedural_ui_baseline.json`（codex 镜像复跑同红）。处理方式：跑一次 `--update-baseline` 收紧基线。
- `dynamic_call_check`、`prep_text_coverage_check`、`asset_manifest_check`、`asset_delivery_check`：与 9.13 记录的成因相同（工作树里大量未提交开发：账号 / 聊天 / 语音 / 好友 / 萝卜营地等整块系统），本轮未处理。

## 部署

- 本批改动**全在客户端**：`data/units/race_units.json`、`scripts/battle/*`、`scripts/ui/UnitDetailFormat.gd`、`scenes/main/Main.gd`、`scenes/menu/*`、`scenes/battle/*`、`officetest/*`。`scripts/autoload/NetworkService.gd` 本轮**未改动**，所以**不需要重启服务端**。
- **但要同步 `data/units/race_units.json` 到服务端与所有客户端**：棋子数值是联机战斗的共享确定性数据，两侧数据表不一致会让同一回合的结算分叉。
- 覆盖方式：把「用于覆盖」目录里的内容**合并**到实际 `project.godot` 所在目录（不要用整个文件夹替换项目，以免丢掉本项目的其它未提交修改）。
- 若你跑的是已导出的 EXE / APK：覆盖源码**不会**改动它，必须**重新导出**。

## 还原

- 本批修改前的原件，以及本批新增文件清单，见桌面备份目录 `GLory-work-9.14修复备份/`：
  - `_原件备份/` 下按原相对路径存放**修改前**的原件（取自 `GLory-codex` 镜像 = 9.13 状态）。
  - `GLory-work/` 下按原相对路径存放**修改后**的文件，便于直接比对。
  - `备份说明.txt`、`校验sha256.txt`（逐文件 sha256，并标注改前/改后是否确实不同）。
- 仅回退本批修改并删除本批新增文件（`docs/9.14bug修复记录.md`）；回退前请先另存当前版本。
- 不处理安装包、存档与远程部署。

## 本批文件清单

**修改（13）**

| 文件 | 对应条目 |
| --- | --- |
| `data/units/race_units.json` | #2、#6、#7 |
| `scripts/battle/BattleSimTreasures.gd` | #1、#2 |
| `scripts/battle/BattleSimSkills.gd` | #5 |
| `scripts/battle/BattleSimulator.gd` | #3、#6 |
| `scripts/ui/UnitDetailFormat.gd` | #2、#3、#4、#5、#6、#7 |
| `scenes/main/Main.gd` | #7 |
| `scenes/menu/MainMenu.gd` | #9 |
| `scenes/menu/Team3v3Lobby.gd` | #10 |
| `scenes/battle/BattleRenderer.gd` | #11、#8 |
| `scenes/battle/BattleArena.gd` | #11 |
| `officetest/OfficeTestScreen.gd` | #8 |
| `tools/four_star_values_check.gd` | 夹具（#2 契约） |
| `README.md` | 本轮小节 |

**新增（1）**

| 文件 | 说明 |
| --- | --- |
| `docs/9.14bug修复记录.md` | 本文件 |

> 清单不含 `.godot/imported/`：那是 Godot 的导入缓存（构建产物），由 `--import` 自动重建，不进版本库、也不进「用于覆盖」目录。
> 本轮**没有**替换任何卡片图 / 贴图，因此不需要 `--import` 重导，也不存在 9.13 #7 那种「图已换、缓存没换」的风险。
